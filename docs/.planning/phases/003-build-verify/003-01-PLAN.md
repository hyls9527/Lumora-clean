---
phase: 003-build-verify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eslint.config.js
  - src/components/CommandPalette.tsx
  - src/components/ExportDialog.tsx
  - src/components/TagManager.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/scroll-area.tsx
  - src/env.d.ts
  - src/lib/api/images.ts
  - src/lib/i18n.tsx
  - src/pages/CurationPage.tsx
  - src/stores/app-store.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "npx tsc --noEmit 输出零错误"
    - "npm run build 成功生成 dist/ 目录"
    - "npx eslint . 输出零错误、零警告"
    - "npm run dev 启动后所有 5 个页面均能正常渲染"
    - "交互功能（搜索、键盘导航、拖拽导入、评分、收藏）正常运行"
  artifacts:
    - path: "dist/index.html"
      provides: "生产构建入口"
    - path: "eslint.config.js"
      provides: "ESLint 配置（含 _ 前缀参数忽略规则）"
  key_links: []
---

<objective>
Phase 003 最终验证门：修复全部 ESLint 错误，确保 TypeScript 零错误编译，生成干净的生产构建，并视觉审查全部 5 个页面。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/001-ui-polish/001-02-SUMMARY.md
@DESIGN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all 25 ESLint errors + 2 warnings across 10 files</name>
  <files>eslint.config.js, src/components/CommandPalette.tsx, src/components/ExportDialog.tsx, src/components/TagManager.tsx, src/components/ui/badge.tsx, src/components/ui/button.tsx, src/components/ui/input.tsx, src/components/ui/scroll-area.tsx, src/env.d.ts, src/lib/api/images.ts, src/lib/i18n.tsx, src/pages/CurationPage.tsx, src/stores/app-store.ts</files>

  <read_first>
    @src/components/CommandPalette.tsx — lines 75, 188-208
    @src/components/ExportDialog.tsx — lines 11, 32-43
    @src/components/TagManager.tsx — line 15
    @src/components/ui/badge.tsx — line 33
    @src/components/ui/button.tsx — line 51
    @src/components/ui/input.tsx — line 5
    @src/components/ui/scroll-area.tsx — lines 5, 25
    @src/env.d.ts — line 1
    @src/lib/api/images.ts — lines 20-36
    @src/lib/i18n.tsx — lines 34, 51
    @src/pages/CurationPage.tsx — line 3
    @src/stores/app-store.ts — lines 48, 115, 122
  </read_first>

  <acceptance_criteria>
    - `npx eslint .` 输出 `✖ 0 problems (0 errors, 0 warnings)`
    - 每次修复均保留原有功能行为
    - 仅修复 ESLint 报告的内容，不进行无关改动
  </acceptance_criteria>

  <action>
按以下顺序修复所有 ESLint 报告的错误和警告。每个文件独立修改，每完成一个文件即运行 `npx eslint {file}` 验证。

**0. eslint.config.js — 添加 argsIgnorePattern 规则**
在 defineConfig 数组的 TypeScript 配置对象中添加 rules 字段：
`  rules: { '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }] }`
这使得 _folderPath、_id、_path 等已加下划线前缀的未使用参数不再报错，与 TypeScript 的 noUnusedParameters 行为一致。

**1. src/components/CommandPalette.tsx — 3 处修复**
- Line 75 (react-hooks/exhaustive-deps): 在 useMemo 依赖数组中添加 openPalette。openPalette 由 useCallback 创建且依赖为空数组，是稳定引用。
- Line 189 (react-hooks/set-state-in-effect): 将 setFocusedIndex(0) 从 useEffect 中移出，改为在 input 的 onChange 中调用。修改 input onChange 为 onChange={(e) => { setQuery(e.target.value); setFocusedIndex(0) }}，然后从 effect 中删除 setFocusedIndex(0) 行。effect 中其余的 setSearchResults 和 setIsSearching 保留（在 setTimeout 回调或条件分支中调用，非同步执行体）。
- Line 208 (react-hooks/exhaustive-deps): 在 useEffect 依赖数组中添加 images。搜索逻辑应随 images 变化重新执行。

**2. src/components/ExportDialog.tsx — 2 处修复**
- Line 11: 从 import 语句中删除 Download。如果该行仅剩 Download 则删除整行。
- Lines 32-43: 用 handleOpenChange 包装函数替换整个 useEffect。创建 useCallback 包装的 handleOpenChange 函数：当 open 为 false 时重置内部状态（format/quality/exporting/progress/intervalRef），然后调用 props.onOpenChange。删除整个 useEffect 块。将 JSX 中所有 onOpenChange={onOpenChange} 替换为 onOpenChange={handleOpenChange}。

**3. src/components/TagManager.tsx — 1 处修复**
- Line 15: 从 import 中删除 Plus 和 Hash，仅保留 `import { X } from "lucide-react"`。验证文件中 X 确实在第 138 行使用。

**4. src/components/ui/badge.tsx — 1 处修复**
- Line 33 (react-refresh/only-export-components): 在 `export { Badge, badgeVariants }` 正上方添加 `// eslint-disable-next-line react-refresh/only-export-components`。

**5. src/components/ui/button.tsx — 1 处修复**
- Line 51 (react-refresh/only-export-components): 在 `export { Button, buttonVariants }` 正上方添加 `// eslint-disable-next-line react-refresh/only-export-components`。

**6. src/components/ui/input.tsx — 1 处修复**
- Line 5 (@typescript-eslint/no-empty-object-type): 将 `export interface InputProps extends React.InputHTMLAttributes<HTMLInputElement> {}` 改为 `export type InputProps = React.InputHTMLAttributes<HTMLInputElement>`。

**7. src/components/ui/scroll-area.tsx — 2 处修复**
- Line 5 (@typescript-eslint/no-empty-object-type): 将 `interface ScrollAreaProps extends React.HTMLAttributes<HTMLDivElement> {}` 改为 `type ScrollAreaProps = React.HTMLAttributes<HTMLDivElement>`。
- Line 25 (@typescript-eslint/no-unused-vars): 将解构中的 `orientation = "vertical"` 删除。orientation 在函数体中未使用（ScrollBar className 为 `"hidden"`，不依赖方向）。接口中的 `orientation?` 声明保留不变。改为 `({ className, ...props }, ref)`。

**8. src/env.d.ts — 1 处修复**
- Line 1 (@typescript-eslint/no-empty-object-type): 在 `interface Window` 正上方添加 `// eslint-disable-next-line @typescript-eslint/no-empty-object-type`。这是 Vite 标准 env.d.ts 模式。

**9. src/lib/api/images.ts — 2 处修复**
- Line 20: 将 `getImages(limit = 50, offset = 0)` 改为 `getImages(_limit = 50, _offset = 0)`。ESLint 配置中 `argsIgnorePattern: "^_"` 规则将处理这些参数。
- 其余已有下划线前缀的参数（_path、_id、_rating、_query）由 eslint.config.js 规则处理。

**10. src/lib/i18n.tsx — 2 处修复**
- Line 34 (@typescript-eslint/no-explicit-any): 将 `let result: any = translations[locale]` 改为 `let result: Record<string, unknown> = translations[locale]`。translations 值为 JSON 对象，通过点号路径遍历。
- Line 51 (react-refresh/only-export-components): 在 `export function useTranslation()` 正上方添加 `// eslint-disable-next-line react-refresh/only-export-components`。useTranslation 是 hook 非组件。

**11. src/pages/CurationPage.tsx — 1 处修复**
- Line 3 (@typescript-eslint/no-unused-vars): 删除 `import { cn } from "@/lib/utils"` 整行。文件中未使用 cn() 函数。

**12. src/stores/app-store.ts — 3 处修复**
- Line 48 (@typescript-eslint/no-unused-expressions): toggleSelect 中的三元表达式改为 if/else：`if (n.has(id)) { n.delete(id) } else { n.add(id) }`。
- Line 115 (@typescript-eslint/no-unused-expressions): toggleTagFilter 中同样的三元表达式改为 if/else。
- Line 122 (@typescript-eslint/no-unused-vars): _folderPath 已有下划线前缀，eslint.config.js 的 `argsIgnorePattern` 规则处理。
  </action>

  <verify>
    <automated>npx eslint . --max-warnings 0</automated>
  </verify>

  <done>npx eslint . 返回 0 problems (0 errors, 0 warnings)。所有 10 个文件修复完成，原有 UI 行为不变。</done>
</task>

<task type="auto">
  <name>Task 2: Full build verification — TypeScript + Vite build + ESLint</name>
  <files>无文件修改 — 纯验证任务</files>

  <read_first>
    @.planning/phases/001-ui-polish/deferred-items.md — 确认所有预存在的 tsc -b 错误已在 Phase 002 中修复
    @tsconfig.app.json — 了解 TypeScript 编译配置
    @package.json — scripts 定义
  </read_first>

  <acceptance_criteria>
    - `npx tsc --noEmit` 返回空输出（零类型错误）
    - `npm run build` 成功退出且 dist/ 目录包含 index.html、CSS bundle、JS bundle
    - `npx eslint . --max-warnings 0` 返回零错误零警告
    - 构建日志中无任何错误信息
  </acceptance_criteria>

  <action>
按顺序执行三个验证命令。任一失败则停止并报告，不继续后续步骤。

**Step 1: TypeScript 类型检查**
执行 `npx tsc --noEmit`。预期：空输出，退出码 0。
如果报错：读取错误信息，分析根本原因（Task 1 引入还是预存在），修复后重试。

**Step 2: 生产构建**
执行 `npm run build`（内部运行 tsc -b && vite build）。
预期：✓ built in Xms，dist/ 目录生成。
验证 dist/ 包含 dist/index.html、dist/assets/index-*.js、dist/assets/index-*.css。
如果 tsc -b 失败而 tsc --noEmit 通过：说明项目引用配置捕获了额外错误，读取并修复。
如果 vite build 失败：读取错误信息，可能涉及未使用导入或模块解析问题。

**Step 3: ESLint 零错误零警告**
执行 `npx eslint . --max-warnings 0`。如果 Task 1 完全通过，此步应直接通过。

三个命令全部通过后，运行 git diff --stat 确认修改范围无意外文件。
  </action>

  <verify>
    <automated>npx tsc --noEmit && npm run build && npx eslint . --max-warnings 0</automated>
  </verify>

  <done>三个命令均以退出码 0 完成。dist/ 目录包含有效的生产构建产物。构建日志干净无错误。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual spot-check — all 5 pages render + interactive flows</name>
  <files>无文件修改 — 纯视觉审查任务</files>

  <read_first>
    @src/App.tsx — 了解页面切换机制（state-based view switching）
    @DESIGN.md — 设计令牌参考，确保视觉一致
    @CLAUDE.md — 反模式检查清单
  </read_first>

  <acceptance_criteria>
    - GalleryPage 渲染网格、图片卡片、标签筛选栏
    - DashboardPage 渲染统计数据面板
    - CurationPage 渲染策展决策界面（图片 + 决策按钮）
    - SettingsPage 渲染设置表单
    - TrashPage 渲染空状态页面
    - 侧边栏导航可点击切换所有 5 个页面（200ms 过渡动画）
    - 所有页面无控制台错误
    - 无 DESIGN.md 反模式（lucide 图标、纯黑/纯白、pill 按钮、Inter 字体等）
  </acceptance_criteria>

  <action>
启动开发服务器并系统性地审查全部 5 个页面。

**Step 1: 启动开发服务器**
执行 `npm run dev`，等待 Vite 输出本地 URL（通常 http://localhost:5173）。

**Step 2: 页面逐一切换验证**
通过点击左侧边栏导航项，依次检查以下页面：

1. **GalleryPage — 默认页面**
   - 图片网格渲染（mock data 200 张图片）
   - ImageCard 显示缩略图、文件名、评分（梅花印 SVG）、收藏状态（藏书印 ◆）
   - TagFilterBar 标签筛选栏渲染
   - 点击图片 → DetailPanel 侧边面板滑出
   - 按 Ctrl+K / Cmd+K 打开 CommandPalette，输入搜索词确认结果高亮

2. **CurationPage — 点击侧边栏策展**
   - 大图 + 三个决策按钮（保留/候选/舍弃）渲染
   - 顶部计数统计实时更新
   - 点击决策按钮后图片切换到下一张

3. **DashboardPage — 点击侧边栏仪表盘**
   - 统计面板渲染
   - 数据可视化元素存在

4. **SettingsPage — 点击侧边栏设置**
   - 设置表单渲染
   - 语言切换（中/英）功能正常
   - 快捷键参考表格显示

5. **TrashPage — 点击侧边栏回收站**
   - 空状态页面渲染（展出尚无弃稿）
   - 藏书印 ◆ 装饰元素存在

**Step 3: 键盘交互验证**
- GalleryPage 中按 ArrowDown/ArrowUp：焦点在图片卡片间移动
- 按 Enter：打开 DetailPanel
- Ctrl+K / Cmd+K：CommandPalette 打开
- CommandPalette 中 ArrowDown/ArrowUp：高亮移动
- Esc：CommandPalette 关闭
- 按 F：切换当前图片收藏状态

**Step 4: 反模式检查清单**
在浏览器 DevTools Console 中执行：
- `document.querySelectorAll('svg.lucide, [class*="lucide"]')` — 结果应为空或仅 TagManager 中的 X icon
- 检查无 #000000 或 #ffffff 色值（Styles 面板搜索）
- 检查无 border-radius: 9999px
- 检查字体为 DM Sans / Noto Serif SC
  </action>

  <verify>
    <human-check>打开浏览器开发者控制台，手动切换全部 5 个页面，确认：无 console.error、所有页面视觉正常、交互功能响应正确</human-check>
  </verify>

  <done>全部 5 个页面正常渲染。侧边栏导航切换流畅。键盘快捷方式功能正常。控制台无错误。无 DESIGN.md 反模式。</done>
</task>

</tasks>

<verification>
Phase 003 最终验证清单：
- [ ] Task 1: 所有 25 个 ESLint 错误 + 2 个警告已修复
- [ ] Task 2: tsc --noEmit 零错误、npm run build 成功、eslint . 零错误零警告
- [ ] Task 3: 全部 5 个页面视觉审查通过
- [ ] dist/ 目录可用于部署
</verification>

<success_criteria>
- TypeScript 编译零错误（tsc --noEmit 和 tsc -b 均通过）
- ESLint 零错误零警告
- 生产构建成功（dist/ 包含所有静态资源）
- 全部 5 个页面在开发服务器中正常渲染
- 核心交互流程（导航、搜索、键盘、评分、收藏）功能正常
- 控制台无 JavaScript 错误
</success_criteria>

<output>
Create `.planning/phases/003-build-verify/003-01-SUMMARY.md` when done
</output>
