---
phase: 002-feature-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CommandPalette.tsx
  - src/i18n/en.json
  - src/i18n/zh.json
autonomous: true
requirements:
  - F-01

must_haves:
  truths:
    - "CommandPalette search debounce is 150ms (not 300ms) when user types in the search input"
    - "CommandPalette search filters locally from store images by matching query against image path (name) and tags — no network call to dead searchImages API"
    - "CommandPalette displays all items as pure text labels with zero lucide-react Icon components rendered"
    - "When search query matches zero commands and zero images, a themed empty state displays the Chinese message '未找到匹配项' with suggestion text '尝试其他关键词，或浏览图库' and a clickable '浏览图库' action button that navigates to gallery"
    - "Grouped results display continues to work: Navigation, Actions, Sort, Import & Export sections appear with section headers; search results section appears only when images match the query"
  artifacts:
    - path: "src/components/CommandPalette.tsx"
      provides: "Command palette with local image search, 150ms debounce, text-only commands, themed empty state"
      contains: "useAppStore"
    - path: "src/i18n/en.json"
      provides: "English strings for search empty state"
      contains: "commandPalette.emptySearch"
    - path: "src/i18n/zh.json"
      provides: "Chinese strings for search empty state"
      contains: "commandPalette.emptySearch"
  key_links:
    - from: "src/components/CommandPalette.tsx"
      to: "src/stores/app-store.ts"
      via: "useAppStore().images for local search filtering"
      pattern: "images\.filter"
    - from: "src/components/CommandPalette.tsx"
      to: "src/stores/app-store.ts"
      via: "setView('gallery') on empty-state browse action"
      pattern: "setView"
---

<objective>
Complete the CommandPalette search feature: switch from dead API search to local store filtering, tighten debounce from 300ms to 150ms, remove all lucide-react icons replacing them with text labels, and add a themed empty state with suggested actions.

Purpose: The existing CommandPalette calls `searchImages()` which returns `[]` — image search is non-functional. The debounce is 300ms instead of the required 150ms. lucide-react icons violate the project anti-patterns established in Phase 001. The empty state is generic ("No results found") instead of the themed "未找到匹配项" message with action guidance required by the user decision.

Output: A fully functional CommandPalette with local image filtering, 150ms debounce, zero lucide-react usage, and a DESIGN.md-aligned empty state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@DESIGN.md
@CLAUDE.md
@.planning/phases/002-feature-completion/002-CONTEXT.md
@src/components/CommandPalette.tsx
@src/stores/app-store.ts
@src/lib/mock-data.ts
@src/i18n/en.json
@src/i18n/zh.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace dead searchImages API with local image filtering + tighten debounce to 150ms</name>
  <files>src/components/CommandPalette.tsx</files>

  <read_first>
    - src/components/CommandPalette.tsx — current search flow (lines 208-229), debounce at 300ms, searchImages import from @/lib/api/images
    - src/stores/app-store.ts — images array type (Image[]), getFilteredImages() pattern for filtering by tags+searchQuery
    - src/lib/mock-data.ts — Image interface (id, path, tags, thumbnail, width, height, etc.)
    - .planning/phases/002-feature-completion/002-CONTEXT.md — search decisions: local filtering only, 150ms debounce
  </read_first>

  <action>
    **Part A — Remove dead searchImages API dependency:**

    1. Delete the import on line 5: `import { searchImages, type ImageRecord } from "@/lib/api/images"`
    2. Delete the `ImageRecord` import from `@/lib/api/images` entirely — it is no longer used by CommandPalette.tsx.

    **Part B — Replace API search with local store filtering:**

    The current search effect (lines 208-229) calls `searchImages(query.trim())` which always returns `[]`. Replace the async search with synchronous local filtering from the Zustand store.

    1. In the store destructuring (lines 45-55), add `images` to the destructured list if not already present (it is, on line 52). Ensure `images` is available in scope.
    2. In the search debounce effect (lines 208-229), replace the entire `setIsSearching(true)` + `debounceRef.current = setTimeout(async () => {...}, 300)` block with synchronous local filtering:

    ```
    debounceRef.current = setTimeout(() => {
      const q = query.trim().toLowerCase()
      const results = images.filter(img =>
        img.path.toLowerCase().includes(q) ||
        img.tags.some(tag => tag.toLowerCase().includes(q))
      ).slice(0, 20)
      setSearchResults(results)
      setIsSearching(false)
    }, 150)
    ```

    Key changes:
    - Debounce `300` → `150` (per user decision)
    - Remove `async` — filtering is synchronous
    - Remove `try/catch` — local filtering cannot throw
    - Filter from `images` (Image[] from store) not from API
    - Limit to 20 results for performance
    - Match against `img.path` (file name) and `img.tags`
    - Set `setIsSearching(true)` before the timeout and `setIsSearching(false)` after filtering completes

    3. Update the `searchResultCommands` useMemo (lines 117-130): the items are now `Image` type (from mock-data.ts), not `ImageRecord` (from api/images.ts). Change:
    - `r.file_path` → `r.path` (Image.path is the file path)
    - Remove `_searchRecord` field assignment (no longer needed since we use Image directly)
    - The `highlightMatch` call in the render section (line 325) currently uses `cmd._searchRecord?.file_path ?? ""` — change to `cmd._searchRecord?.path ?? ""` where `_searchRecord` is now typed as `Image`

    **Part C — Type cleanup:**

    1. Update the `Command` interface (lines 24-32): change `_searchRecord?: ImageRecord` to `_searchRecord?: Image` (import Image from `@/lib/mock-data` — but it's already imported indirectly... no, `Image` is imported from lucide-react on line 6! This is a naming conflict. Rename the lucide-react `Image` icon import to avoid collision, or remove it entirely — the icon will be removed in Task 2.)

    For now, keep the import since Task 2 handles icon removal. Add the `Image as ImageType` rename from lucide-react, or just handle it in Task 2. For Task 1, the searchResults are Image[] (from store) — the mapping in searchResultCommands creates items whose `_searchRecord` is already typed as `any` implicitly since the store returns `Image` type. No explicit type change needed in this task — just update the render-time access from `file_path` to `path`.

    Actually, the simplest approach: the `searchResultCommands` useMemo maps over `searchResults` which is now `Image[]`. The existing code accesses `r.file_path` which doesn't exist on `Image`. Change line 121: `hint: r.file_path.split(/[\\\\/]/).pop()` → `hint: r.path.split(/[\\\\/]/).pop()`. And line 325: `cmd._searchRecord?.file_path ?? ""` → `cmd._searchRecord?.path ?? ""`.

    **Verification after changes:**
    - `npx tsc --noEmit` — zero NEW TypeScript errors in CommandPalette.tsx
  </action>

  <verify>
    <automated>grep -c "searchImages" src/components/CommandPalette.tsx</automated>
    <automated>grep -c "from \"@/lib/api/images\"" src/components/CommandPalette.tsx</automated>
    <automated>grep -n "}, 150)" src/components/CommandPalette.tsx</automated>
    <automated>grep -c "images\.filter" src/components/CommandPalette.tsx</automated>
    <automated>grep -c "file_path" src/components/CommandPalette.tsx</automated>
    <automated>npx tsc --noEmit 2>&1 | grep -c "CommandPalette"</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -c "searchImages" src/components/CommandPalette.tsx` returns 0
    - Source assertion: `grep -c "from \"@/lib/api/images\"" src/components/CommandPalette.tsx` returns 0
    - Source assertion: `grep -n "}, 150)" src/components/CommandPalette.tsx` shows the setTimeout delay value of 150 (not 300)
    - Source assertion: `grep -c "images\.filter" src/components/CommandPalette.tsx` returns >= 1 (local filtering is present)
    - Source assertion: `grep -c "file_path" src/components/CommandPalette.tsx` returns 0 (ImageRecord.file_path replaced with Image.path)
    - Source assertion: `grep -c "async.*searchImages\|await.*searchImages" src/components/CommandPalette.tsx` returns 0 (no async API call in search flow)
    - Behavior: `npx tsc --noEmit` exits with zero NEW TypeScript errors in CommandPalette.tsx
    - Behavior: `npm run dev` — CommandPalette search shows image results when typing image names or tags from mock data
  </acceptance_criteria>

  <done>CommandPalette search filters locally from store images (path + tags) with 150ms debounce. No dead searchImages API call remains. ImageRecord type and @/lib/api/images import removed from CommandPalette.tsx.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove all lucide-react icons — replace with text labels</name>
  <files>src/components/CommandPalette.tsx</files>

  <read_first>
    - src/components/CommandPalette.tsx — current state after Task 1, lucide-react import block (lines 6-22), Icon usage in render (lines 307, 323)
    - DESIGN.md — anti-patterns list: "❌ lucide-react 图标"
    - CLAUDE.md — rule: "导航和操作用纯文字，不用 lucide-react 图标"
    - src/i18n/en.json — command translations under commandPalette.commands.* for text labels
  </read_first>

  <action>
    **Part A — Remove lucide-react import:**

    Delete the entire lucide-react import block (lines 6-22):
    ```
    import { Image, Sparkles, BarChart3, Settings, Trash2, CheckSquare, X, ArrowDownAZ, ArrowUpAZ, HardDrive, FolderPlus, Download, Search, Tag, Star } from "lucide-react"
    ```

    **Part B — Remove Icon rendering from command items:**

    The `Command` interface (line 28) has `icon: React.ElementType`. Remove this field entirely — commands no longer carry icon references.

    In the `commands` useMemo (lines 77-94), remove the `icon` property from every command object. Delete all lines like `icon: Image,`, `icon: Sparkles,`, `icon: BarChart3,`, etc.

    In the `searchResultCommands` useMemo (line 122), remove `icon: Image,` from the mapped object.

    **Part C — Remove Icon rendering from the JSX:**

    In the render section (lines 307-308), remove the `Icon` variable and its render:
    ```
    const Icon = cmd.icon                                    // DELETE
    ...
    <Icon className="w-4 h-4 shrink-0 opacity-50" />         // DELETE
    ```

    The button content (lines 311-332) becomes:
    ```
    <button ...>
      <span className="flex-1 truncate">
        {isSearch ? highlightMatch(...) : t(cmd.labelKey)}
      </span>
      {cmd.hint && (...)}
    </button>
    ```

    The `gap-2.5` in the button className (line 313) can be reduced to `gap-0` or removed since there's only one flex child now. Change `"w-full flex items-center gap-2.5 px-3 py-2 ..."` → `"w-full flex items-center px-3 py-2 ..."`.

    **Part D — Clean up unused imports:**

    Run `npx tsc --noEmit` to identify any remaining lucide-react references. If Task 1 already removed the `Image` import for the icon (which was the badly-named import conflicting with the data type), ensure no `Image` from lucide is still referenced.

    **Verification after changes:**
    - Zero lucide-react imports in CommandPalette.tsx
    - Zero `icon:` properties in command definitions
    - Zero Icon rendering in JSX
    - `npx tsc --noEmit` passes with zero errors in CommandPalette.tsx
  </action>

  <verify>
    <automated>grep -c "lucide-react" src/components/CommandPalette.tsx</automated>
    <automated>grep -c "icon:" src/components/CommandPalette.tsx</automated>
    <automated>grep -c "Icon" src/components/CommandPalette.tsx</automated>
    <automated>grep -c "lucide-react" src/components/TagManager.tsx src/components/ExportDialog.tsx</automated>
    <automated>npx tsc --noEmit 2>&1 | grep -i "CommandPalette"</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -c "lucide-react" src/components/CommandPalette.tsx` returns 0
    - Source assertion: `grep -c "icon:" src/components/CommandPalette.tsx` returns 0 (zero `icon:` property assignments in command definitions)
    - Source assertion: `grep -c "Icon " src/components/CommandPalette.tsx` returns 0 (zero `<Icon ...>` JSX elements)
    - Source assertion: `grep -c "icon\?" src/components/CommandPalette.tsx` returns 0 (Command interface has no icon field)
    - Note: TagManager.tsx and ExportDialog.tsx still use lucide-react — this is expected and outside Phase 002 scope. Run `grep -c "lucide-react" src/components/TagManager.tsx src/components/ExportDialog.tsx` to confirm count is 2 (not 0) — these are deferred to a future phase.
    - Behavior: `npx tsc --noEmit` exits with zero TypeScript errors in CommandPalette.tsx
    - Behavior: `npm run dev` — CommandPalette opens with ⌘K, all command items display as text labels, keyboard navigation works
  </acceptance_criteria>

  <done>CommandPalette.tsx has zero lucide-react imports, zero Icon JSX elements, zero `icon:` property assignments. All commands display as pure text labels matching the project's no-icon design rule.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Themed empty search state with suggested actions + i18n keys</name>
  <files>src/components/CommandPalette.tsx, src/i18n/en.json, src/i18n/zh.json</files>

  <read_first>
    - src/components/CommandPalette.tsx — current state after Task 1 and Task 2, empty state at lines 293-296
    - src/i18n/en.json — existing commandPalette section for reference
    - src/i18n/zh.json — existing commandPalette section for reference
    - DESIGN.md — color tokens, typography (font-serif for poetic text), transition spec (200ms ease-out)
    - .planning/phases/002-feature-completion/002-CONTEXT.md — decision: empty state shows "未找到匹配项" with suggested actions (try other keywords / browse gallery)
  </read_first>

  <action>
    **Part A — Add i18n keys:**

    In `src/i18n/en.json`, add a new section under `commandPalette`:
    ```
    "emptySearch": {
      "title": "No matching items found",
      "subtitle": "Try different keywords, or browse the gallery",
      "action": "Browse Gallery"
    }
    ```

    In `src/i18n/zh.json`, add a new section under `commandPalette`:
    ```
    "emptySearch": {
      "title": "未找到匹配项",
      "subtitle": "尝试其他关键词，或浏览图库",
      "action": "浏览图库"
    }
    ```

    Insert these after the existing `commandPalette.hints` object but before the closing `}` of the commandPalette section.

    **Part B — Replace empty state UI:**

    The current empty state (lines 293-296):
    ```
    {flatCommands.length === 0 ? (
      <div className="px-4 py-6 text-center text-[13px] text-text-muted font-serif">
        {isSearching ? t("gallery.loading") : t("commandPalette.noResults")}
      </div>
    ```

    Replace with a themed empty state that distinguishes between "no query yet" (all commands visible, filtered list is empty because query matches nothing) and "active search with zero results":

    ```
    {flatCommands.length === 0 ? (
      isSearching ? (
        <div className="px-4 py-6 text-center text-[13px] text-text-muted font-serif">
          {t("gallery.loading")}
        </div>
      ) : query.trim() ? (
        <div className="px-6 py-8 text-center space-y-3">
          <div className="w-12 h-12 rounded-[2px] bg-bg border border-border-subtle flex items-center justify-center mx-auto mb-1">
            <span className="font-serif text-[18px] text-text-faint">?</span>
          </div>
          <h3 className="font-serif text-[14px] text-text">
            {t("commandPalette.emptySearch.title")}
          </h3>
          <p className="font-serif text-[12px] text-text-muted leading-relaxed">
            {t("commandPalette.emptySearch.subtitle")}
          </p>
          <button
            onClick={() => { closePalette(); setView("gallery") }}
            className="mt-2 px-4 py-1.5 rounded-[4px] font-serif text-[12px] bg-accent/8 text-accent hover:bg-accent/12 transition-all duration-200 ease-out"
          >
            {t("commandPalette.emptySearch.action")}
          </button>
        </div>
      ) : (
        <div className="px-4 py-6 text-center text-[13px] text-text-muted font-serif">
          {t("commandPalette.noResults")}
        </div>
      )
    ```

    Three cases handled:
    1. `isSearching` — show loading state (existing "研墨中…" / "Preparing...")
    2. `query.trim()` has content but zero results — show themed empty state with title, subtitle, and browse action
    3. No query and zero results (shouldn't happen with commands always present, but fallback) — show generic no results

    **Part C — Ensure closePalette and setView are accessible in the empty state scope:**

    The `closePalette` and `setView` functions are already available in the component. The button onClick calls both `closePalette()` and `setView("gallery")` — both are in scope.

    **Verification:**
    - i18n keys resolve correctly with `t()`
    - Empty state renders only when search query produces zero matches
    - Browse button navigates to gallery and closes palette
    - `npx tsc --noEmit` passes
  </action>

  <verify>
    <automated>grep -c "emptySearch" src/i18n/en.json</automated>
    <automated>grep -c "emptySearch" src/i18n/zh.json</automated>
    <automated>grep -c "commandPalette.emptySearch.title" src/components/CommandPalette.tsx</automated>
    <automated>grep -c "commandPalette.emptySearch.action" src/components/CommandPalette.tsx</automated>
    <automated>grep -c "closePalette.*setView" src/components/CommandPalette.tsx</automated>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -c "emptySearch" src/i18n/en.json` returns >= 1; same for `src/i18n/zh.json`
    - Source assertion: Both en.json and zh.json contain `emptySearch.title`, `emptySearch.subtitle`, `emptySearch.action` keys
    - Source assertion: `grep -c "commandPalette.emptySearch.title" src/components/CommandPalette.tsx` returns 1
    - Source assertion: `grep -c "commandPalette.emptySearch.action" src/components/CommandPalette.tsx` returns 1
    - Source assertion: The empty state button onClick calls both `closePalette()` and `setView("gallery")` — `grep "onClick.*closePalette\|onClick.*setView" src/components/CommandPalette.tsx` shows the combined action
    - Behavior: Opening CommandPalette and typing gibberish that matches zero items shows the themed "未找到匹配项" state with a browse-gallery button
    - Behavior: Clicking the browse button closes the palette and navigates to gallery view
    - Behavior: `npx tsc --noEmit` exits with zero TypeScript errors (no new errors from CommandPalette.tsx or i18n files)
  </acceptance_criteria>

  <done>CommandPalette empty search state displays themed "未找到匹配项" / "No matching items found" with suggestion text and a clickable browse action that navigates to gallery. i18n keys added to both en.json and zh.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Search input | User-typed query string — displayed as text, used for local filtering; no network transmission, no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-002-01 | Tampering | npm dependencies | accept | No new packages installed — only code modifications to existing source files. Package legitimacy not applicable. |
| T-002-02 | Information Disclosure | CommandPalette search | accept | Search is local filtering of mock data — no user data, no backend, no API. Zero information disclosure surface. |
</threat_model>

<verification>
Overall plan checks after ALL tasks complete:
1. `grep -c "searchImages" src/components/CommandPalette.tsx` returns 0
2. `grep -c "lucide-react" src/components/CommandPalette.tsx` returns 0
3. `grep -c "icon:" src/components/CommandPalette.tsx` returns 0
4. `grep -n "}, 150)" src/components/CommandPalette.tsx` shows debounce at 150
5. `grep -c "emptySearch" src/i18n/en.json` returns >= 1
6. `grep -c "emptySearch" src/i18n/zh.json` returns >= 1
7. `npx tsc --noEmit` exits with zero NEW errors in CommandPalette.tsx
</verification>

<success_criteria>
- [ ] CommandPalette search filters locally from store images (path + tags) — no dead API call
- [ ] Search debounce is exactly 150ms (not 300ms)
- [ ] Zero lucide-react imports or JSX elements in CommandPalette.tsx
- [ ] All command items display as pure text labels
- [ ] Themed empty state renders "未找到匹配项" with suggestion text and browse-gallery button when search matches nothing
- [ ] Browse button navigates to gallery and closes the palette
- [ ] i18n keys (commandPalette.emptySearch.*) present in both en.json and zh.json
- [ ] TypeScript compiles cleanly (no new errors)
</success_criteria>

<output>
Create `.planning/phases/002-feature-completion/002-01-SUMMARY.md` when done
</output>
