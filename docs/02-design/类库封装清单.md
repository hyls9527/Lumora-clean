# 类库封装清单 — Lumora

> **审计原则**：每个类库函数必须追溯到具体代码文件和行号。

## 1. Rust 后端类库

### 1.1 数据库层 (db/)

#### DbHandle

**文件**：`src-tauri/src/db/mod.rs`

**职责**：数据库连接管理

| 方法 | 签名 | 说明 |
|------|------|------|
| `open` | `fn open(path: &Path) -> Result<DbHandle>` | 打开数据库连接 |
| `conn` | `fn conn(&self) -> MutexGuard<Connection>` | 获取连接 |

---

#### 数据库迁移

**文件**：`src-tauri/src/db/migrations.rs`

| 函数 | 签名 | 说明 |
|------|------|------|
| `migrate` | `fn migrate(conn: &Connection) -> Result<()>` | 执行所有迁移 |

**测试覆盖**：4 tests（幂等迁移、版本检查、表存在性、降级测试）

---

#### Schema 定义

**文件**：`src-tauri/src/db/schema.rs`

| 常量 | 说明 |
|------|------|
| `V1_CREATE_IMAGES` | 图片表 SQL |
| `V1_FTS5` | FTS5 虚表 SQL |
| `V2_CREATE_TAGS` | 标签表 SQL |
| `V2_CREATE_IMAGE_TAGS` | 关联表 SQL |
| `V3_ADD_DELETED_AT` | 回收站列 SQL |
| `V4_CREATE_EMBEDDINGS` | 嵌入表 SQL |
| `V4_CREATE_VEC_TABLE` | 向量表 SQL |
| `V5_CREATE_ANALYSIS_HISTORY` | 分析历史表 SQL |
| `V6_CREATE_VARIANT_GROUPS` | 变体组表 SQL |

---

### 1.2 类型定义层 (schema/)

**文件**：`src-tauri/src/schema/types.rs`

#### 核心类型

| 类型 | 字段数 | 说明 |
|------|--------|------|
| `ImageRecord` | 14 | 图片记录（与 images 表 1:1） |
| `PaginatedResult` | 4 | 分页结果包装 |
| `Tag` | 4 | 标签记录 |
| `DashboardStats` | 6 | 仪表盘统计 |
| `FormatCount` | 2 | 格式统计 |
| `RatingCount` | 2 | 评分统计 |
| `TagCount` | 2 | 标签统计 |
| `ExportResult` | 3 | 导出结果 |
| `ImportResult` | 4 | 导入结果 |

#### 辅助函数

| 函数 | 签名 | 说明 |
|------|------|------|
| `row_to_record` | `fn row_to_record(row: &Row) -> Result<ImageRecord>` | SQLite 行→ImageRecord |

**代码依据**：`src-tauri/src/schema/types.rs`

---

### 1.3 错误处理层 (error.rs)

**文件**：`src-tauri/src/error.rs`

#### AppError 枚举

| 变体 | 数据 | 说明 |
|------|------|------|
| `Db` | `String` | 数据库错误 |
| `Io` | `String` | 文件 I/O 错误 |
| `NotFound` | `String` | 记录未找到 |
| `InvalidInput` | `String` | 输入验证失败 |
| `External` | `String` | 外部服务错误 |
| `Lock` | - | 锁失败 |

#### 自动转换

| 来源 | 目标 |
|------|------|
| `rusqlite::Error` | `AppError::Db` |
| `std::io::Error` | `AppError::Io` |
| `reqwest::Error` | `AppError::External` |
| `serde_json::Error` | `AppError::External` |

**类型别名**：`AppResult<T> = Result<T, AppError>`

---

### 1.4 元数据解析层 (metadata/)

**文件**：`src-tauri/src/metadata/mod.rs`

#### 核心函数

| 函数 | 签名 | 说明 |
|------|------|------|
| `probe_metadata_from_bytes` | `fn(buf: &[u8], ext: &str) -> Option<String>` | 从字节缓冲区提取元数据 JSON |

**文件**：`src-tauri/src/metadata/png.rs`

| 函数 | 签名 | 说明 |
|------|------|------|
| `read_text_chunks_from_bytes` | `fn(buf: &[u8]) -> Option<Vec<(String, String)>>` | 读取 PNG tEXt/iTXt 块 |

**文件**：`src-tauri/src/metadata/sd.rs`

| 函数 | 签名 | 说明 |
|------|------|------|
| `parse_metadata` | `fn(chunks: &[(String, String)]) -> Option<String>` | 解析 SD/ComfyUI 参数为 JSON |

**测试覆盖**：12 tests（A1111 参数、ComfyUI prompt、无元数据、非 PNG）

---

### 1.5 Ollama 集成层 (ollama.rs)

**文件**：`src-tauri/src/ollama.rs`

#### OllamaConfig 结构

| 方法 | 签名 | 说明 |
|------|------|------|
| `from_env` | `fn from_env() -> Self` | 从环境变量读取配置 |
| `host` | `fn host(&self) -> &str` | 获取主机 URL |
| `url` | `fn url(&self, path: &str) -> String` | 构建完整 URL |

#### Tauri 命令

| 命令 | 签名 | 说明 |
|------|------|------|
| `get_ollama_host` | `fn(config: State<OllamaConfig>) -> String` | 返回 Ollama 地址 |
| `check_ollama_status` | `async fn(config: State<OllamaConfig>) -> Result<(bool, Option<String>)>` | 检查 Ollama 状态 |

---

### 1.6 LAN 服务器层 (lan_server.rs)

**文件**：`src-tauri/src/lan_server.rs`

| 函数 | 签名 | 说明 |
|------|------|------|
| `start_server` | `fn(conn: Arc<Mutex<Connection>>) -> u16` | 启动 LAN 服务器，返回端口 |

**测试覆盖**：2 tests（端口分配、本地 IP）

---

### 1.7 命令层 (commands/)

#### 图片命令 (commands/images/)

**文件**：`src-tauri/src/commands/images/import.rs`

| 命令 | 签名 | 说明 |
|------|------|------|
| `import_images` | `fn(db: State<DbHandle>, path: String) -> AppResult<ImportResult>` | 导入图片 |

**内部函数**：

| 函数 | 说明 |
|------|------|
| `scan_folder` | 递归扫描文件夹 |
| `insert_image` | 插入单张图片 |
| `load_record` | 加载插入的记录 |

**文件**：`src-tauri/src/commands/images/ops.rs`

| 命令 | 说明 |
|------|------|
| `list_images` | 分页列表 |
| `update_rating` | 更新评分 |
| `toggle_favorite` | 切换收藏 |
| `list_favorites` | 收藏列表 |
| `rebuild_fts_index` | 重建 FTS 索引 |
| `get_variant_group_images` | 获取变体组图片 |

**文件**：`src-tauri/src/commands/images/search.rs`

| 命令 | 说明 |
|------|------|
| `search_images` | FTS5 全文搜索 |
| `search_images_advanced` | 高级搜索（按字段） |

**测试覆盖**：10 tests

---

#### 标签命令 (commands/tags.rs)

| 命令 | 说明 |
|------|------|
| `create_tag` | 创建标签 |
| `list_tags` | 列出标签 |
| `delete_tag` | 删除标签 |
| `update_tag` | 更新标签 |
| `add_tag_to_image` | 添加标签到图片 |
| `remove_tag_from_image` | 移除图片标签 |
| `get_image_tags` | 获取图片标签 |

**测试覆盖**：4 tests

---

#### 回收站命令 (commands/trash.rs)

| 命令 | 说明 |
|------|------|
| `soft_delete_image` | 软删除 |
| `restore_image` | 恢复 |
| `permanent_delete_image` | 永久删除 |
| `list_trash` | 回收站列表 |
| `empty_trash` | 清空回收站 |
| `batch_soft_delete` | 批量软删除 |
| `batch_restore` | 批量恢复 |
| `batch_permanent_delete` | 批量永久删除 |
| `batch_add_tag` | 批量添加标签 |
| `batch_remove_tag` | 批量移除标签 |

**测试覆盖**：3 tests

---

#### AI 分析命令 (commands/ai.rs)

**数据结构**：

| 类型 | 字段 | 说明 |
|------|------|------|
| `AnalysisTag` | name, confidence | AI 标签建议 |
| `AnalysisResult` | description, tags, objects, color_palette, composition | 分析结果 |
| `AnalysisHistoryItem` | id, image_id, result, analyzed_at | 历史记录 |

| 命令 | 说明 |
|------|------|
| `analyze_image_cmd` | AI 分析图片 |
| `get_analysis_result_cmd` | 获取最新分析 |
| `get_analysis_history_cmd` | 获取分析历史 |
| `apply_ai_tags_cmd` | 应用 AI 标签 |

**测试覆盖**：4 tests

---

#### 嵌入命令 (commands/embeddings.rs)

**数据结构**：

| 类型 | 字段 | 说明 |
|------|------|------|
| `EmbeddingInfo` | status, dimensions, generated_at | 嵌入状态 |
| `SemanticSearchResult` | id, similarity | 搜索结果 |

**内部函数**：

| 函数 | 说明 |
|------|------|
| `upsert_embedding` | 插入/更新嵌入 |
| `get_embedding_status_db` | 查询嵌入状态 |
| `get_embedding_stats_db` | 获取嵌入统计 |
| `search_similar` | 向量相似度搜索 |

| 命令 | 说明 |
|------|------|
| `generate_embedding` | 存储嵌入 |
| `get_embedding_status_cmd` | 查询状态 |
| `search_semantic_cmd` | 语义搜索 |
| `get_embedding_stats_cmd` | 嵌入统计 |
| `embed_text_cmd` | 文本→向量 |
| `generate_embedding_for_image_cmd` | 生成图片嵌入 |

**测试覆盖**：6 tests

---

#### CLIP 命令 (commands/clip.rs)

| 命令 | 说明 |
|------|------|
| `clip_embed_image_cmd` | CLIP 图片嵌入 |
| `clip_embed_text_cmd` | CLIP 文本嵌入 |

**测试覆盖**：1 test

---

#### 其他命令

| 文件 | 命令 | 说明 |
|------|------|------|
| `commands/dashboard.rs` | `get_dashboard_stats` | 仪表盘统计 |
| `commands/export.rs` | `export_images` | 导出图片 |
| `commands/settings.rs` | `get_setting`, `set_setting` | 设置 CRUD |
| `commands/backup.rs` | `export_database`, `import_database` | 数据库备份 |

---

## 2. TypeScript 前端类库

### 2.1 Tauri Bridge (lib/tauri.ts)

**文件**：`src/lib/tauri.ts`

| 函数 | 签名 | 说明 |
|------|------|------|
| `invoke` | `<T>(cmd: string, args?: Record<string, unknown>) => Promise<T>` | 安全的 Tauri invoke 封装 |
| `onWriteCommand` | `(cb: () => void) => () => void` | 注册写命令回调 |

**设计决策**：
- Browser 模式返回 mock 数据
- Tauri 模式调用真实 API
- 写命令自动通知监听器（缓存失效）

---

### 2.2 API 层 (lib/api/)

#### images.ts

**文件**：`src/lib/api/images.ts`

| 函数 | 签名 | 说明 |
|------|------|------|
| `toImageRecord` | `(raw: TauriImageRecord) => ImageRecord` | 原始记录→前端类型 |
| `importImages` | `(folderPath: string) => Promise<ImportResult>` | 导入图片 |
| `listImages` | `(page: number, perPage: number) => Promise<PaginatedResult>` | 分页列表 |
| `searchImages` | `(query: string) => Promise<ImageRecord[]>` | FTS5 搜索 |
| `searchImagesAdvanced` | `(query: string, field?: string) => Promise<ImageRecord[]>` | 高级搜索 |
| `updateRating` | `(id: string, rating: number) => Promise<void>` | 更新评分 |
| `toggleFavorite` | `(id: string) => Promise<void>` | 切换收藏 |
| `listFavorites` | `() => Promise<ImageRecord[]>` | 收藏列表 |
| `listTrash` | `(page: number, perPage: number) => Promise<PaginatedResult>` | 回收站列表 |
| `softDeleteImage` | `(id: string) => Promise<void>` | 软删除 |
| `restoreImage` | `(id: string) => Promise<void>` | 恢复 |
| `permanentDeleteImage` | `(id: string) => Promise<void>` | 永久删除 |
| `emptyTrash` | `() => Promise<number>` | 清空回收站 |
| `exportImages` | `(ids: string[], destDir: string, format: string, renameTemplate?: string) => Promise<ExportResult>` | 导出图片 |

**测试覆盖**：16 tests

---

#### ai.ts

**文件**：`src/lib/api/ai.ts`

| 函数 | 签名 | 说明 |
|------|------|------|
| `analyzeImage` | `(imageId: string, imagePath?: string, model?: string) => Promise<AnalysisResult>` | AI 分析 |
| `getAnalysisResult` | `(imageId: string) => Promise<AnalysisResult \| null>` | 获取分析结果 |
| `getAnalysisHistory` | `(imageId: string) => Promise<AnalysisHistoryItem[]>` | 分析历史 |
| `batchAutoTag` | `(imageIds: string[], onProgress?: (progress: BatchTagProgress) => void) => Promise<BatchTagResult>` | 批量自动标签 |

**辅助函数**：

| 函数 | 说明 |
|------|------|
| `snakeToCamel` | snake_case → camelCase |
| `convertKeysToCamel` | 递归键名转换 |
| `normalizeAnalysisResult` | 标准化分析结果 |

---

#### embeddings.ts

**文件**：`src/lib/api/embeddings.ts`

| 函数 | 签名 | 说明 |
|------|------|------|
| `getEmbeddingStatus` | `(imageId: string) => Promise<EmbeddingInfo>` | 嵌入状态 |
| `generateEmbeddings` | `(images: ImageRecord[]) => Promise<void>` | 批量生成嵌入（并发=3） |
| `getEmbeddingStats` | `() => Promise<EmbeddingStats>` | 嵌入统计 |

---

#### semantic.ts

**文件**：`src/lib/api/semantic.ts`

| 函数 | 签名 | 说明 |
|------|------|------|
| `searchSemantic` | `(query: string, limit?: number) => Promise<SemanticSearchResult[]>` | 文本语义搜索 |
| `generateImageEmbedding` | `(imageId: string, description: string) => Promise<void>` | 生成图片嵌入 |
| `searchByImage` | `(filePath: string, limit?: number, excludeId?: string) => Promise<SemanticSearchResult[]>` | 以图搜图 |

**流程**：
- 文本搜索：`embed_text_cmd` → `search_semantic_cmd`
- 以图搜图：`clip_embed_image_cmd` → `search_semantic_cmd`

---

#### semanticCache.ts

**文件**：`src/lib/api/semanticCache.ts`

| 函数 | 签名 | 说明 |
|------|------|------|
| `getCachedResult` | `(query: string) => SemanticSearchResult[] \| null` | 获取缓存 |
| `setCachedResult` | `(query: string, results: SemanticSearchResult[]) => void` | 设置缓存 |
| `invalidateCache` | `() => void` | 清除缓存 |

**配置**：
- TTL: 30 分钟
- 最大条目: 200
- 持久化: localStorage
- 防抖: 300ms

**测试覆盖**：8 tests

---

#### backup.ts

**文件**：`src/lib/api/backup.ts`

| 函数 | 签名 | 说明 |
|------|------|------|
| `exportDatabase` | `(destination: string) => Promise<string>` | 导出数据库 |
| `importDatabase` | `(source: string) => Promise<string>` | 导入数据库 |

---

#### lan.ts

**文件**：`src/lib/api/lan.ts`

| 函数 | 签名 | 说明 |
|------|------|------|
| `getLanInfo` | `() => Promise<{ port: number; ip: string }>` | 获取 LAN 信息 |

---

### 2.3 工具库 (lib/)

#### tokens.ts

**文件**：`src/lib/tokens.ts`

| 导出 | 类型 | 说明 |
|------|------|------|
| `t` | object | Design tokens (bg, text, accent, etc.) |
| `labelStyle` | CSSProperties | 标签样式 |
| `valueStyle` | CSSProperties | 值样式 |

---

#### i18n.ts

**文件**：`src/lib/i18n.ts`

| 函数 | 签名 | 说明 |
|------|------|------|
| `t` | `(key: string, lang?: Language, params?: Record<string, string \| number>) => string` | 独立翻译函数 |
| `useTranslation` | `(namespace?: string) => { t, lang }` | React Hook |
| `notifyLanguageChanged` | `() => void` | 通知语言变更 |

**支持语言**：zh, en

---

#### format.ts

**文件**：`src/lib/format.ts`

| 函数 | 签名 | 说明 |
|------|------|------|
| `formatDate` | `(iso: string) => string` | 格式化日期 (zh-CN) |
| `formatFileSize` | `(kb: number) => string` | 格式化文件大小 (KB/MB/GB) |

---

### 2.4 自定义 Hooks (hooks/)

| Hook | 文件 | 签名 | 说明 |
|------|------|------|------|
| `useImageActions` | `hooks/useImageActions.ts` | `() => { toggleFavorite, setRating }` | 图片操作（乐观更新） |
| `useSelection` | `hooks/useSelection.ts` | `() => { selectedIds, toggleSelect, selectAll, clearSelection }` | 批量选择 |
| `useOllamaStatus` | `hooks/useOllamaStatus.ts` | `() => { available, checking, error, recheck }` | Ollama 状态轮询 (60s) |
| `useKeyboardNav` | `hooks/useKeyboardNav.ts` | `(items, onSelect) => { focusedIndex, handleKeyDown }` | 键盘导航 |
| `useImageSrc` | `hooks/useImageSrc.ts` | `(filePath: string) => string` | 图片路径→可显示 URL |
| `useMediaQuery` | `hooks/useMediaQuery.ts` | `() => boolean` | 媒体查询（移动端检测） |
| `useDragDrop` | `hooks/useDragDrop.ts` | `({ onDrop }) => { isDragging }` | 拖拽导入 |

**测试覆盖**：5 hooks 有测试

---

## 3. API 命名对照表

| 前端 API 函数 | Tauri IPC 命令 | Rust 函数 |
|---------------|---------------|-----------|
| `importImages` | `import_images` | `commands::images::import_images` |
| `listImages` | `list_images` | `commands::images::list_images` |
| `searchImages` | `search_images` | `commands::images::search_images` |
| `searchImagesAdvanced` | `search_images_advanced` | `commands::images::search_images_advanced` |
| `updateRating` | `update_rating` | `commands::images::update_rating` |
| `toggleFavorite` | `toggle_favorite` | `commands::images::toggle_favorite` |
| `analyzeImage` | `analyze_image_cmd` | `commands::ai::analyze_image_cmd` |
| `searchSemantic` | `embed_text_cmd` + `search_semantic_cmd` | `commands::embeddings::*` |
| `searchByImage` | `clip_embed_image_cmd` + `search_semantic_cmd` | `commands::clip::*` + `commands::embeddings::*` |
| `exportImages` | `export_images` | `commands::export::export_images` |
| `getDashboardStats` | `get_dashboard_stats` | `commands::dashboard::get_dashboard_stats` |
| `exportDatabase` | `export_database` | `commands::backup::export_database` |
| `importDatabase` | `import_database` | `commands::backup::import_database` |

---

## 4. 测试覆盖统计

| 模块 | 测试文件数 | 测试数 |
|------|-----------|--------|
| Rust commands | 10 | 28 |
| Rust db | 1 | 4 |
| Rust metadata | 3 | 12 |
| Rust lan_server | 1 | 2 |
| **Rust 总计** | **15** | **59** |
| TS stores | 9 | 98 |
| TS lib/api | 7 | 72 |
| TS hooks | 4 | 34 |
| TS components | 6 | 72 |
| TS lib | 1 | 6 |
| **TS 总计** | **27** | **282** |

---

*文档生成时间：基于 commit 69983af6*
