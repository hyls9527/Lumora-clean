# 版本验收报告 — Lumora v0.8.0

> **验收日期**：2026-08-02
> **验收对象**：Lumora v0.8.0（tag `v0.8.0`）
> **审计原则**：所有数据来自实际测试运行结果，不做推算。

## 1. 版本信息

| 项目 | 值 |
|------|-----|
| 版本号 | v0.8.0 |
| 版本主题 | 体验打磨 + 交付质量保障 |
| 平台 | Windows / macOS / Linux（Tauri 2） |
| 发布方式 | GitHub Release（tag 触发 release.yml，三平台构建 + 自动更新元数据） |

## 2. 测试结果（2026-08-02 实测）

### 2.1 前端测试（vitest + jsdom）

- **672 / 672 通过**（90 个测试文件），通过率 100%
- 覆盖率（v8）：语句 **75.86%** / 分支 **78.13%** / 函数 **61.12%**
- CI 覆盖率阈值：70% / 70% / 55% → ✅ 全部达标
- `tsc --noEmit`：0 错误
- i18n：zh/en 键对等（i18nParity 测试通过）

### 2.2 后端测试（Rust）

- **128 / 128 通过**，0 失败
- `cargo fmt --check`：0 差异
- `cargo clippy --all-targets -- -D warnings`：0 警告
- `cargo check`：通过

### 2.3 质量门禁汇总

| 门禁 | 数值 | 结果 |
|------|------|------|
| 前端测试 | 672/672 | ✅ |
| 覆盖率 | 75.86 / 78.13 / 61.12 | ✅ |
| Rust 测试 | 128/128 | ✅ |
| tsc / fmt / clippy | 0 错误 0 警告 | ✅ |
| perf-budget | 6/6 | ✅ |
| npm audit / cargo audit | 0 已知漏洞（CI security-audit） | ✅ |

## 3. 功能验收摘要

| 功能 | 状态 | 说明 |
|------|------|------|
| 变体对比视图 | ✅ | 同 prompt 不同 seed 并排对比（2-6 张） |
| 高级筛选面板 | ✅ | 模型/日期/评分/尺寸组合，防抖刷新 |
| 批量重命名 | ✅ | 模板预览 + 冲突处理 |
| 批量格式转换 | ✅ | PNG↔WebP↔JPEG |
| 图片信息增强 | ✅ | 完整 SD 参数展示 |
| 性能优化 | ⚠️ | 虚拟滚动实现并测试；TC-PERF-002（10K 滚动帧率）需人工验收 |
| 键盘快捷键 | ✅ | ⌘K/⌘I/⌘R/Esc + 帮助面板 |
| 错误恢复 | ✅ | 离线提示、加载失败重试、ErrorBoundary 懒加载兜底 |
| 移动端适配 | ✅ | MobileNav + 响应式（真机/LAN 实测待闭环） |
| LAN 移动访问 | ✅ | 带鉴权，恒定时间 token 比较 |
| 导入健壮性 | ✅ | 符号链接防循环、内容哈希去重、时间戳异常回退 |

## 4. 缺陷修复记录（v0.8.0 交付周期）

| 缺陷 | 优先级 | 修复提交 |
|------|--------|---------|
| 生产库未执行迁移 | P1 | 6a14167 |
| 高级筛选接线死代码 | P1 | e857cb6 |
| 导入符号链接循环 / 内容哈希 / 时间戳 panic | P1 | e857cb6 |
| LAN 一次绑定复用 / 鉴权加固 | P1 | e857cb6 |
| 灰色地带（Esc/⌘R/拖拽/筛选语义/范围校验等） | P1/P2 | 834a2a9 |
| 设置页撑高视口导致侧边栏搜索框被裁切 | P1 | b8d0a13 |
| 版本号不一致（0.7.0/0.7.1 vs 0.8.0） | P2 | 95222d8 |
| 收藏页 setTimeout 刷新竞态 | P2 | 95222d8 |
| 导出页选区语义不明确 | P2 | 95222d8 |
| CI 测试 teardown 竞态（DashboardPage） | P1 | 发布前修复 |

每项修复均通过单元测试或回归验证，未引入新风险。

## 5. 遗留项与建议

- **TC-PERF-002**：10K 图片滚动帧率 ≥ 30fps 需人工验收后勾选
- **语义搜索 / 以图搜图**：依赖 Ollama/CLIP 与向量索引，需真实环境端到端实测
- **独立子 Agent 审计**：因 DeepSeek 余额不足（402）未执行，已由主代理按 review-agent 规范回退审查替代
- **移动端 LAN 真机实测**：代码与自动化测试已覆盖，建议发布后用手机走一遍

## 6. 结论

**Lumora v0.8.0 达到发布标准，准予发布。** 全部自动化门禁通过，功能缺陷已闭环，剩余项均为人工验收或外部服务实测类，不影响版本发布。
