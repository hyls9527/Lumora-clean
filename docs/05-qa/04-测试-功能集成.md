# 测试轮 1 — 功能测试与集成测试（2026-08-02）

## 1. 范围

- 功能测试：全部核心业务场景（导入/列表/搜索/评分收藏/标签/回收站/AI 分析/嵌入/导出/重命名/转换/筛选/变体/快捷键/离线/路由）
- 集成测试：IPC 契约、写命令语义、API 转换层、页面级数据流

## 2. 执行结果

| 套件 | 结果 | 说明 |
|------|------|------|
| 前端全量（vitest） | **660 通过 / 0 失败**（30.5s） | 83+ 文件 |
| 集成/契约子集 | **94 通过 / 0 失败** | type-contract、write-commands、toImageRecord、images、semanticCache、searchByImage、comfyui、ImportPage、GalleryPage、TrashPage |
| Rust 全量（cargo test --lib） | **124 通过 / 0 失败** | 含迁移、过滤、转换、重命名、CLIP、元数据解析 |
| 类型检查 | tsc 0 错误 | - |

## 3. 覆盖的核心业务场景

导入（拖拽/文件夹/去重）→ 列表（分页/排序/筛选）→ 搜索（全文/字段/语义/以图搜图）→ 评分收藏（乐观更新/回滚）→ 标签（单图/批量）→ 回收站（软删/恢复/清空）→ AI 分析（建议/接受拒绝/历史）→ 嵌入（生成/状态/统计）→ 导出/重命名/转换（预览/冲突/失败）→ 变体对比 → 离线恢复。

## 4. 发现缺陷

本轮未发现功能缺陷；集成层面 1 项遗留风险转交安全/兼容轮核查（浏览器 mock 数据完整性，见测试轮 3 COMP-1）。

## 5. 结论

功能与集成层达到交付标准，无阻断缺陷。
