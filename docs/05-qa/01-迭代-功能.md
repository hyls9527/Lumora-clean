# 迭代轮 1 — 功能完整性（2026-08-02）

## 1. 迭代目标

以「初始交付标准」为基线，聚焦 v0.8.0 核心功能模块的**功能完整性与逻辑正确性**，消除交互缺口与版本交付断点。

## 2. 迭代范围

| 模块 | 范围 |
|------|------|
| 批量格式转换（ConvertDialog） | Esc 关闭、焦点行为、错误反馈 |
| 批量重命名（RenameDialog） | Esc 关闭、弹窗栈一致性、错误反馈 |
| 弹窗体系 | 与 DetailModal / CommandPalette / ShortcutsPanel 的栈语义一致 |
| 版本交付 | 版本号、CHANGELOG、验收报告同步 |

## 3. 审计发现与修复

| # | 发现 | 严重度 | 修复 |
|---|------|--------|------|
| F-1 | ConvertDialog 无 Esc 关闭，仅支持遮罩点击 | P2 | 接入 `useModalEsc(open, onClose, dialogRef)`，获得栈感知 Esc + 焦点陷阱 |
| F-2 | RenameDialog 的 Esc 使用裸 window 监听，未参与弹窗栈（与 DetailModal 叠加时会连锁关闭） | P2 | 改为 `useModalEsc`，统一栈语义 |
| F-3 | RenameDialog 执行失败静默（catch 为空），与 ConvertDialog 已修复行为不一致 | P2 | 增加 `role="alert"` 错误展示 + `rename.error` i18n 键（zh/en 同步） |
| F-4 | 版本号仍为 0.7.1，无 v0.8.0 CHANGELOG | P3 | package.json / Cargo.toml / Cargo.lock 升至 0.8.0；CHANGELOG 增补 v0.8.0 全量条目 |

## 4. 验收标准与结果

| 验收项 | 标准 | 结果 |
|--------|------|------|
| 弹窗 Esc 行为 | 任一弹窗按 Esc 只关闭自身（顶层），不连锁 | ✅ 全部接入 useModalEsc（新增重命名/转换两个入口） |
| 失败可见 | 执行失败展示错误且不触发 onComplete | ✅ 新增测试覆盖 |
| 版本一致性 | package.json / Cargo.toml / Cargo.lock 三处一致 | ✅ 0.8.0 |
| 回归 | 全量测试无回归 | ✅ 定向 27 测试通过；tsc 0 错误；cargo 124 通过 |

## 5. 架构稳定性确认

- 本轮未引入新模块，全部复用既有 `useModalEsc` / `modalStack` 模式，符合 CLAUDE.md「复用现有，不创造接口」。
- i18n 键新增 2 个（zh/en 各 1），键集合仍严格对齐（377 → 379）。
- 技术架构稳定：无依赖变更、无数据模型变更、无 IPC 契约变更。

## 6. 新增/变更测试

- `RenameDialog.test.tsx`：`shows an error alert when the rename execution fails`
- 既有 `ConvertDialog.test.tsx` / `useModalEsc.test.ts` 回归通过
