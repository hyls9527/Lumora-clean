# 测试轮 2 — 性能测试与安全测试（2026-08-02）

## 1. 性能测试

| 指标 | 实测 | 预算/标准 | 结果 |
|------|------|-----------|------|
| 构建产物 | 0.4MB | ≤0.5MB | ✅ |
| npm 依赖数 | 320 | ≤350 | ✅ |
| Rust 二进制 | 22.5MB | ≤30MB | ✅ |
| cargo crates | 605 | ≤700 | ✅ |
| TS 文件数 | 186 | ≤200 | ✅ |
| Zustand stores | 12 | ≤12 | ✅ |
| 批量插入 1000 张 | 101ms | - | ✅ |
| 分页查询 40 条/1000 张 | 310µs | - | ✅ |
| 前端全量测试 | 30.5s | - | ✅ |

## 2. 安全测试

| # | 检查项 | 方法 | 结果 |
|---|--------|------|------|
| SEC-1 | 依赖漏洞 | `npm audit`（官方 registry） | ❌ 发现 **2 高危**（postcss ≤8.5.17，GHSA-r28c-9q8g-f849 路径遍历）→ `npm audit fix` 后 **0 漏洞** ✅ |
| SEC-2 | 硬编码密钥 | 全仓正则扫描（api key/secret/password/私钥/ghp_/sk-） | ✅ 0 命中 |
| SEC-3 | 权限最小化 | `capabilities/default.json` | ✅ 仅 `core:default` + `dialog:default` |
| SEC-4 | SQL 注入 | 代码审查：全部筛选/搜索参数化 | ✅ 无字符串拼接用户输入 |
| SEC-5 | 路径穿越 | `sanitize_filename` 单测（`..`、绝对路径字符、非法字符） | ✅ 3 测试通过 |
| SEC-6 | 命令面 | Tauri invoke_handler 仅注册白名单命令 | ✅ |

## 3. 残余风险

- `cargo audit` 未执行（环境未安装 cargo-audit），依赖侧仅覆盖 npm 生态；建议 CI 补充。
- npm 审计需使用官方 registry（默认镜像源不支持 audit 接口），已记录到 CI 注意事项。

## 4. 结论

性能全部达标；安全缺陷 SEC-1 已修复并复验为 0 漏洞，其余检查项通过。
