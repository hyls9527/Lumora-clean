# 全量数据真实性自检报告（2026-08-02）

> 说明：Lumora 为本地单机应用，仓库内不含用户业务数据（图片/评分/标签等均存于用户本地数据库）。本自检覆盖**系统自带数据**：配置、i18n、Schema、常量、文档声明，并核查数据处理逻辑。

## 1. 检查项与结果

| # | 数据项 | 检查方法 | 结果 |
|---|--------|----------|------|
| D-1 | i18n 数据 | en/zh 键集合比对脚本 | ✅ 386 = 386，无缺失/多余键 |
| D-2 | i18n 值 | 抽样核对 + 组件硬编码扫描 | ✅ 无硬编码中文（设计字形「梅花」除外，已确认） |
| D-3 | Schema 版本 | `SCHEMA_VERSION=6` vs `apply_v1..v6` 六段迁移 | ✅ 一致 |
| D-4 | 迁移执行路径 | 生产 `open()` 与测试 `open_memory()` | ✅ 均执行 `run_migrations`（此前 P1 已修复并有测试） |
| D-5 | LAN 端口 | `lan_server.rs`（8079..8090）vs mock（8079）vs 测试断言 | ✅ 一致 |
| D-6 | Ollama 默认地址 | `ollama.rs` 默认 `http://localhost:11434` vs mock | ✅ 一致 |
| D-7 | 版本号 | package.json / Cargo.toml / Cargo.lock | ✅ 均为 0.8.0 |
| D-8 | 测试数据契约 | `mockData.test.ts`（浏览器模式） | ✅ 23 断言锁定形状 |
| D-9 | 文档声明 | README 测试数量 | ❌ 发现过时（430/59）→ 已更新为 660/124 ✅ |
| D-10 | 文档声明 | ARCHITECTURE.md 命令清单 vs `lib.rs` | ⚠️ 架构文档为历史快照，命令清单为子集；已核对新命令已注册（`is_directory` 等） |
| D-11 | 数据处理逻辑 | SD 参数解析容错、标签数组容错、嵌入状态机 | ✅ 单测覆盖 |
| D-12 | 用户数据 | 仓库内无用户数据 | ✅ 无需核查（本地应用） |

## 2. 异常数据项

| 数据项 | 问题 | 处理结论 |
|--------|------|----------|
| README 测试数量 | 声称 430 前端 / 59 Rust，实际 660 / 124 | 已修正 |

## 3. 结论

系统数据准确性、完整性、一致性、合规性核查通过；发现 1 项文档数据异常并已闭环。
