# Lumora 交付质量保障（QA）记录

> **适用版本**：v0.12.0（2026-09-18）

## 现行记录

| 文档 | 内容 | 状态 |
|------|------|------|
| [12-深度审计缺陷清单.md](./12-深度审计缺陷清单.md) | 全仓只读审计：60 项发现（P1×10 / P2×30 / P3×20），含复现与根因 | 已闭环 |
| [13-修复交付记录.md](./13-修复交付记录.md) | 逐条修复映射（审计 ID → 改动位置 → 回归测试） | 已交付 |
| [14-商业级交付验收矩阵.md](./14-商业级交付验收矩阵.md) | **验收依据**：九项指标逐条实测值 + 可复现命令 + 环境限制 | 现行 |

## 质量基线（2026-09-17/18 本机实测）

- 前端测试：**972 通过 / 0 失败**（113 文件；语句·行 87.79%、分支 81.43%）
- Rust 测试：**285 通过 / 0 失败 / 5 ignored**（ignored 需本机 Ollama）；行覆盖 80.21%
- `tsc --noEmit` 0 错误；`cargo fmt --check` 通过；`cargo clippy -D warnings` 0 警告
- npm 高危漏洞 0（`npm audit --json`）；perf-budget 全部达标
- 页面加载 TC-PERF-001 < 2s；API P95 < 300ms；RTO/RPO 演练通过

> 数字以 [14-商业级交付验收矩阵.md](./14-商业级交付验收矩阵.md) 为准 —— 其他文档若与此不一致，以验收矩阵和当次实测命令输出为准。

## 历史记录

v0.8.0 时代的三轮迭代/测试/自检报告（原 `01`~`10`）与分支代码审查（原 `11`）已移除：其中的测试数、Schema 版本、覆盖率与当前状态矛盾，保留会误导验收。需要查阅时用 git 历史取回：

```bash
git log --diff-filter=D --name-only -- 'docs/05-qa/*'   # 找到删除提交
git show <commit>^:docs/05-qa/10-最终交付报告.md        # 取回单份文档
```
