# 缺陷系统修复与验证（2026-08-02）

## 1. 缺陷清单与修复状态

| ID | 缺陷 | 严重度 | 修复 | 验证 |
----|------|--------|------|------|
| F-1 | ConvertDialog 无 Esc 关闭 | P2 | 接入 `useModalEsc` | 定向测试 + 回归 |
| F-2 | RenameDialog Esc 未参与弹窗栈 | P2 | 接入 `useModalEsc` | 同上 |
| F-3 | RenameDialog 执行失败静默 | P2 | `role="alert"` 错误展示 + i18n | 新增失败用例 |
| F-4 | 版本 0.7.1 未随 v0.8.0 功能发布 | P3 | 三处版本号 + CHANGELOG | 一致性校验 |
| UX-1~4 | 硬编码中文（Trash 13 处/404/相对时间/加载失败） | P2/P3 | 全部 i18n 化（新增 8 键） | 全仓审计 + 386=386 |
| UX-6 | 弹窗交互不一致 | P2 | 对话框统一 Esc/焦点陷阱 | 定向测试 |
| PERF-1/2 | 日期筛选索引失效 | P2 | 半开区间 + 索引友好条件 | 既有测试全过 |
| SEC-1 | postcss ≤8.5.17 高危（路径遍历） | **P1** | `npm audit fix` | 复验 0 漏洞 |
| COMP-1 | 浏览器 mock 数据缺失致 dev 崩溃 | P1 | 补齐 mock 契约 | 新增 mockData 测试 23 断言 |

## 2. 修复原则

- 每个缺陷先补/跑失败用例（RED），修复后验证（GREEN），再全量回归。
- 修复均复用既有模式（`useModalEsc`、i18n 键、参数化 SQL），未引入新架构。

## 3. 回归验证

全部修复完成后执行全量回归：前端 660、Rust 124、tsc 0、fmt/clippy 0、构建 + perf-budget 全绿（详见 09 回归验证）。

## 4. 结论

9 项缺陷全部闭环；无遗留未修复缺陷（残余风险仅 2 项外部工具类建议：cargo-audit、CI 实跑确认）。
