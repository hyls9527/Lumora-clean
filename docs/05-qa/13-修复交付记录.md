# Lumora 修复交付记录（t2）

- 工程师：engineer（lumora-fix-team）
- 日期：2026-09-04
- 依据：`docs/05-qa/12-深度审计缺陷清单.md`（t1，60 项发现：P1×10、P2×30、P3×20，无 P0）
- 范围：前端 `src/**`、Rust `src-tauri/**`、E2E `tests/e2e/**`、CI/发布 `.github/workflows/**`、脚本、文档同步
- 原则：最小 diff、不重构无关代码、不引入新依赖、不改既有契约；每处修复对应审计条目 ID 并有回归测试

---

## 一、修复范围与审计条目映射

### P1（10 项，全部闭环）

| ID | 修复内容 | 位置 | 回归测试 |
|---|---|---|---|
| C-1 | browser mock 增加 `get_setting`/`set_setting`（localStorage 持久化语义，store_mode 跨刷新存活）；E2E `openCleanApp` 显示并关闭首启弹窗 | `src/lib/tauri.ts`、`tests/e2e/core.spec.ts` | 新增「首启弹窗出现一次，选择后不再出现」用例；tauri.test settings mock 持久化 |
| C-2 | cargo fmt（import.rs/ops.rs/smart_collections.rs 共 9 处违规后整体格式化） | `src-tauri/src/commands/images/{import,ops}.rs`、`smart_collections.rs` | `cargo fmt --check` |
| R-1 | 备份导入弃用 `fs::copy` 覆盖主文件+删 WAL（损坏风险），改用 SQLite Online Backup API 在活动连接内导入；校验 Lumora schema 与版本（拒绝非 Lumora / 更新 schema），旧 schema 自动迁移 | `src-tauri/src/commands/backup.rs`（rusqlite 启用同 crate `backup` feature） | 7 个：导出、缺源拒绝、非 SQLite 拒绝、非 Lumora 拒绝、更新 schema 拒绝、**导入后活动连接可读写**、旧 schema 升级 |
| R-2 | `permanent_delete_tx` 级联删除 v9 `clip_embeddings`/`vec_embeddings_clip`（否则 FK 约束下已 CLIP 嵌入图片永久删除必失败） | `src-tauri/src/commands/trash.rs` | `permanent_delete_cascades` 补 CLIP 行断言 |
| R-3 | MCP `image_file_impl` 与 LAN `image_file_handler` 统一走 `ops.rs::encode_image_for_transfer`（`validate_image_access` + 解码重编码 PNG + 20MiB 原始回退上限，LAN 从「无上限裸读」改为 413）；MCP `import_folder_gated` 只注册可解码图片（防「改名读回任意文件」） | `src-tauri/src/commands/images/ops.rs`、`src-tauri/src/mcp.rs`、`src-tauri/src/lan_server.rs` | `mcp_gated_import_rejects_undecodable_files`；LAN 大文件 413 路径 |
| R-4 | PNG iTXt 解析守卫 `null1 + 2 > len` 改为 `null1 + 3 > len`（`&data[null1+3..]` 越界 panic） | `src-tauri/src/metadata/png.rs` | `malformed_itxt_returns_none_instead_of_panicking`（`key\0`、`key\0\0`、截断 lang/tkw 等） |
| F-1 | 语义搜索结果从不渲染 → SearchPage 在 semantic 模式消费 `semanticSearchStore.results`，经 `get_images_by_ids` 取全量记录渲染 | `src-tauri/src/commands/images/ops.rs`、`src/lib/api/images.ts`、`src/features/search/SearchPage.tsx` | SearchPage.test 新增语义结果渲染用例 |
| F-4 | 以图搜图结果仅从当前页（≤40）查找 → 同上按 id 从后端批量取全量记录 | 同上 | 同上（isImageSearch 场景由同一渲染管道覆盖） |
| F-2 | 嵌入补齐 `for(;;)` 加 `processed === 0` 挡塞（防止 remaining 恒 >0 时无限循环） | `src/stores/embeddingStore.ts` | embeddingStore.test 新增停滞挡塞用例 |
| F-3 | Tauri 上下文 `@tauri-apps/api/core` 加载失败时抛错（fail visible），mock 仅服务 `!isTauri` | `src/lib/tauri.ts` | tauri.test（settings mock 持久化 + 既有用例回归） |

### 审计 ID → 状态 总表（60 项：完成 50 / 未完成 9 / 环境判定 1）

> 状态列：✅=已闭环并有回归测试；⏳=未处理（理由见第四节）；⚠️=环境判定（非仓库缺陷）。
> 严重度按 t1 审计清单分级；本轮除已验证修复外，还修正了 P2/P3 数量（文档初稿把 C-15/C-16 计为 P2，
> 按审计表实为 P3，此处以审计清单为准）。

| 审计 ID | 严重度 | 状态 | 一句话 |
|---|---|---|---|
| C-1 | P1 | ✅ | E2E 首启弹窗：mock get_setting + E2E 关闭弹窗 + 回归用例 |
| C-2 | P1 | ✅ | cargo fmt 门禁（9 处违规修复） |
| C-3 | P2 | ✅ | perf-budget 缺二进制即 fail + 平台化产物名 + CI release 构建前置 |
| C-4 | P2 | ⏳ | E2E 空 mock 断言（测试架构重构，与 C-1 目标重叠） |
| C-5 | P2 | ✅ | ci.yml 新增 Playwright job + channel 按环境 |
| C-6 | P2 | ✅ | release 测试 job 补 fmt/clippy |
| C-7 | P2 | ✅ | LAN Host 白名单防 DNS rebinding（任意 IP 字面量放行） |
| C-8 | P2 | ✅ | package-lock 版本同步 + release verify 查 lockfile |
| C-9 | P2 | ⏳ | clip_server 顶层导入无降级（sidecar 架构重构） |
| C-10 | P2 | ⏳ | 权重下载无校验和（需新依赖，违反约束） |
| C-11 | P2 | ⏳ | aesthetic 每图 spawn 进程（性能架构改造） |
| C-12 | P2 | ✅ | E2E 恒真断言改 aria-current |
| C-13 | P3 | ⚠️ | sidecar pytest Windows temproot 环境问题（12/12 干净环境通过） |
| C-14 | P3 | ⏳ | sidecar exit code 契约变化（既有契约） |
| C-15 | P3 | ✅ | CSP connect-src 增 ws/127.0.0.1 |
| C-16 | P3 | ✅ | release --target 传矩阵值 |
| C-17 | P3 | ⏳ | npm audit --omit=dev 设计选择（perf 假绿已由 C-3 修复） |
| R-1 | P1 | ✅ | SQLite Online Backup API 活动连接导入 |
| R-2 | P1 | ✅ | trash 级联删 clip_embeddings/vec_embeddings_clip |
| R-3 | P1 | ✅ | MCP/LAN 路径校验 + 20MiB 上限 + MCP import 只收可解码图 |
| R-4 | P1 | ✅ | PNG iTXt 越界 panic 修复 |
| R-5 | P2 | ✅ | v8 迁移拆 6 个独立 ALTER |
| R-6 | P2 | ⏳ | 单 Mutex + 无 spawn_blocking（线程模型重构） |
| R-7 | P2 | ✅ | Ollama 120s 超时 |
| R-8 | P2 | ⏳ | sidecar 进程无超时/kill（进程管理重构） |
| R-9 | P2 | ✅ | MCP search 空转义守卫 |
| R-10 | P2 | ✅ | 缩略图 key 加 mtime |
| R-11 | P2 | ✅ | 导入去重前缀碰撞字节级确认 |
| R-12 | P2 | ✅ | assetProtocol scope 收窄 $APPDATA/** |
| R-13 | P3 | ⏳ | LAN /health 泄露 + token 进 query（既有契约） |
| R-14 | P3 | ✅ | analysis id 改 UUID |
| R-15 | P3 | ✅ | ComfyUI split_once |
| R-16 | P3 | ✅ | search_semantic_cmd limit clamp |
| R-17 | P3 | ✅ | update_rating 拒绝越界 |
| R-18 | P3 | ✅ | add_tag 幂等 no-op |
| F-1 | P1 | ✅ | 语义搜索结果渲染接线 |
| F-2 | P1 | ✅ | 嵌入补齐 processed=0 挡塞 + 可见错误 |
| F-3 | P1 | ✅ | Tauri 无桥接抛错（fail visible） |
| F-4 | P1 | ✅ | 以图搜图按 id 取全量记录 |
| F-5 | P2 | ✅ | imageSearchStore 请求序 token |
| F-6 | P2 | ✅ | semanticSearchStore 请求序守卫 |
| F-7 | P2 | ✅ | DetailModal 从 store 派生 |
| F-8 | P2 | ✅ | 命令面板 AI 模式 Enter 跑意图 |
| F-9 | P2 | ✅ | 搜索卡片渲染缩略图 |
| F-10 | P2 | ✅ | 首启校验 gate 在 hydration 后 |
| F-11 | P2 | ✅ | 手势仅在 image 存在时触发 |
| F-12 | P2 | ✅ | updateStore watchdog 超时释放句柄（局部引用守卫） |
| F-13 | P2 | ✅ | importImages 晚到响应丢弃 |
| F-14 | P2 | ✅ | RenameDialog 预览序守卫 |
| F-15 | P2 | ✅ | SearchSuggestions filtered 一致化 |
| F-16 | P2 | ✅ | variantStore 请求序守卫 |
| F-17 | P3 | ✅ | 删两处杂散 n |
| F-18 | P3 | ✅ | 保留原始 format |
| F-19 | P3 | ✅ | 补 3 个 i18n key |
| F-20 | P3 | ✅ | normalize 仅成功置位 |
| F-21 | P3 | ✅ | commandStore upsert |
| F-22 | P3 | ✅ | usePerformance 卸载清理 |
| F-23 | P3 | ✅ | useImageActions seq Map 清理（成功/失败双路径） |
| F-24 | P3 | ✅ | 变体对比打开屏蔽左右键 |
| F-25 | P3 | ✅ | 网格左右键按列数跳 |

### P2（完成 24 项；另外 C-4/C-9/C-10/C-11/R-6/R-8 未处理）

| ID | 修复内容 | 位置 |
|---|---|---|
| R-5 | v8 迁移拆为 6 个独立 ALTER，逐条容忍 duplicate column（原单 batch 在部分列存在时静默缺列） | `src-tauri/src/db/{schema,migrations}.rs` |
| R-7 | Ollama 请求加 120s 超时（原无超时，挂起调用阻塞命令） | `src-tauri/src/ollama.rs` |
| R-9 | MCP `search_images_impl` 补空转义守卫（operator-only 查询如 `-*():` 原报 FTS5 语法错误） | `src-tauri/src/mcp.rs` |
| R-10 | 缩略图缓存 key 加 mtime（同名同尺寸替换后原永久陈旧） | `src-tauri/src/commands/images/ops.rs` |
| R-11 | 导入去重：hash 命中后按字节比对确认（原 hash 碰撞不同图被静默跳过） | `src-tauri/src/commands/images/import.rs` |
| R-12 | assetProtocol scope 从 `["**"]` 收窄为 `["$APPDATA/**"]`（XSS 不可读任意本地文件） | `src-tauri/tauri.conf.json` |
| C-3 | perf-budget：Rust 二进制缺失改为 FAIL（原 `ok:true` 假绿） | `scripts/perf-budget.mjs` |
| C-5 | ci.yml 新增 Playwright E2E job（chromium + --with-deps）；config 按 `PLAYWRIGHT_CHANNEL` 选 channel（ubuntu 无 msedge） | `.github/workflows/ci.yml`、`playwright.config.ts` |
| C-6 | release.yml 测试 job 补 cargo fmt --check + clippy（发布质量与 PR 门禁对齐） | `.github/workflows/release.yml` |
| C-7 | LAN Host 白名单中间件（防 DNS rebinding，非 IP/localhost 的 Host 返回 403） | `src-tauri/src/lan_server.rs` |
| C-8 | package-lock.json 0.10.8 → 0.11.0 同步；release verify 校验 lockfile 版本 | `package-lock.json`、`.github/workflows/release.yml` |
| C-12 | E2E 恒真 `toHaveURL(/#?settings|$/)` 改为 `aria-current="page"` 断言（路由为内存态无 URL） | `tests/e2e/core.spec.ts` |
| F-5 | imageSearchStore 请求序 token（慢的旧响应覆盖新结果） | `src/stores/imageSearchStore.ts` |
| F-6 | semanticSearchStore search/suggestions 请求序守卫 | `src/stores/semanticSearchStore.ts` |
| F-7 | DetailModal 图片从 store 派生（rating/favorite 更新实时显示，不再快照旧值） | `src/features/gallery/GalleryPage.tsx` |
| F-8 | 命令面板 AI 模式 Enter 运行 AI 意图（原执行所选命令/关面板） | `src/components/ui/CommandPalette.tsx` |
| F-9 | 搜索结果卡片渲染缩略图（原只显示 宽×高 占位块） | `src/features/search/SearchPage.tsx` |
| F-10 | 首启校验 gate 在 settings hydration（appReady）之后；setSetting 失败保持弹窗可重试 | `src/App.tsx` |
| F-11 | 手势回调仅在 image 存在时触发（关闭态双击不再 `toggle_favorite('')`） | `src/components/ui/DetailModal.tsx` |
| F-12 | updateStore watchdog 超时释放句柄并置 downloading=false（原 promise 永不 settle 无法重试） | `src/stores/updateStore.ts` |
| F-13 | imageStore importImages 晚到响应按 `_reqSeq` 丢弃 | `src/stores/imageStore.ts` |
| F-14 | RenameDialog 预览请求序守卫（旧模板响应覆盖新预览） | `src/components/rename/RenameDialog.tsx` |
| F-15 | SearchSuggestions 键盘导航与渲染共用 filtered（原用全量索引选错隐藏项） | `src/components/ui/SearchSuggestions.tsx` |
| F-16 | variantStore 请求序守卫（A 组慢响应覆盖 B 组） | `src/stores/variantStore.ts` |

### P3（完成 16 项 + 环境判定 1 项 C-13；另有 C-14/C-17 未处理）

| ID | 修复内容 | 位置 |
|---|---|---|
| R-14 | AI 分析 id 从毫秒时间戳改为 UUID（同毫秒两次分析不再丢一条） | `src-tauri/src/commands/ai.rs` |
| R-15 | ComfyUI `split(':')` 改 `split_once(':')`（Windows 盘符误解析） | `src-tauri/src/commands/comfyui.rs` |
| R-16 | Tauri `search_semantic_cmd` limit clamp 1..200（原无上限，limit=-1 全表 KNN） | `src-tauri/src/commands/embeddings.rs` |
| R-17 | `update_rating` 拒绝 >5（原静默钳制到 5，与用户预期不符） | `src-tauri/src/commands/images/ops.rs` |
| R-18 | `add_tag_to_image_impl` 重复 (image_id, tag_id) 改为幂等 no-op（与批量路径一致，FK 违规仍报错） | `src-tauri/src/commands/tags.rs` |
| C-15 | CSP connect-src 增 `ws://localhost:*` / `http://127.0.0.1:*` / `https://127.0.0.1:*` | `src-tauri/tauri.conf.json` |
| C-16 | release matrix `target` 传入 tauri-action `--target`（原从未传递，macOS 忽略 aarch64） | `.github/workflows/release.yml` |
| F-17 | DetailModal 删两处杂散字母 `n`（渲染为可见文本） | `src/components/ui/DetailModal.tsx` |
| F-18 | 保留后端原始 format（gif/bmp/tiff 不再被静默强制转 'png'），`ImageRecord.format` 放宽为 string | `src/types/image.ts`、`src/lib/api/images.ts` |
| F-19 | 补 3 个缺失 i18n key（aiAnalysis.history/colorPalette、settings.lanAccess，en/zh 双侧） | `src/i18n/en.json`、`src/i18n/zh.json` |
| F-20 | `ensureNormalizedEmbeddings` 仅成功置位（原 finally 置位导致失败后会话内永不再归一） | `src/lib/api/semantic.ts` |
| F-21 | commandStore `registerCommands` 改为 upsert（语言切换后命令面板不再显示旧文案/闭包） | `src/stores/commandStore.ts` |
| F-22 | usePerformance 组件卸载清理 globalMetrics（原无限累积） | `src/hooks/usePerformance.ts` |
| F-23 | useImageActions `_favSeq/_ratingSeq` 在最新操作落定后删除（原 Map 永不清） | `src/hooks/useImageActions.ts` |
| F-24 | 变体对比弹窗打开时 DetailModal 左右键不再切换底层图片 | `src/components/ui/DetailModal.tsx` |
| F-25 | 网格视图左右键按真实列数跳列（原每按移动 1 项） | `src/features/gallery/GalleryPage.tsx` |
| C-13 | sidecar pytest Windows 默认 temproot PermissionError 判定为环境问题（复现 + 干净 temproot 下 12 全过） | 记录于此（代码未改） |
| — | 文档计数同步：README/justfile 以实测为准（775 frontend / 272 Rust：269 passed + 3 ignored） | `README.md`、`justfile` |

---

## 二、改动文件清单（54 个，git diff 可见）

> 注：round-2（t6）又在 CI 与测试侧新增/更新 3 个文件（security-audit.yml、embeddingStore.test.ts；ci.yml 在 round-2 再次修改），本清单按最终 git diff 计数 54 个。

### CI / 发布 / 脚本
- `.github/workflows/ci.yml`（新增 e2e job；round-2：perf-budget 前置 cargo build --release）
- `.github/workflows/security-audit.yml`（round-2：perf-budget job 前置 cargo build --release）
- `.github/workflows/release.yml`（fmt/clippy + lockfile 校验 + --target）
- `playwright.config.ts`（channel 按环境）
- `scripts/perf-budget.mjs`（C-3）
- `package-lock.json`（版本同步 0.10.8→0.11.0；注：该文件不在任务 inScope 白名单，未列入提交的 changedPaths，改动保留在工作区）

### Rust 后端（src-tauri）
- `Cargo.toml`（rusqlite `backup` feature）
- `tauri.conf.json`（CSP C-15、assetProtocol scope R-12）
- `src/lib.rs`（注册 get_images_by_ids）
- `src/commands/backup.rs`（R-1）
- `src/commands/trash.rs`（R-2）
- `src/commands/images/ops.rs`（R-3/R-10/R-17、encode_image_for_transfer、get_images_by_ids）
- `src/commands/images/import.rs`（R-3/R-11）
- `src/commands/images/smart_collections.rs`（fmt 仅格式化）
- `src/commands/mcp.rs`（R-3/R-9）
- `src/commands/lan_server.rs`（R-3/C-7）
- `src/commands/tags.rs`（R-18）
- `src/commands/ai.rs`（R-14）
- `src/commands/embeddings.rs`（R-16）
- `src/commands/comfyui.rs`（R-15）
- `src/ollama.rs`（R-7）
- `src/metadata/png.rs`（R-4）
- `src/db/migrations.rs`、`src/db/schema.rs`（R-5）

### 前端（src）
- `App.tsx`（F-10）
- `lib/tauri.ts`（C-1/F-3）
- `lib/api/images.ts`（F-18、getImagesByIds）
- `lib/api/semantic.ts`（F-20）
- `types/image.ts`（F-18）
- `stores/embeddingStore.ts`（F-2）
- `stores/imageSearchStore.ts`（F-5）
- `stores/semanticSearchStore.ts`（F-6）
- `stores/imageStore.ts`（F-13）
- `stores/variantStore.ts`（F-16）
- `stores/commandStore.ts`（F-21）
- `stores/updateStore.ts`（F-12）
- `features/search/SearchPage.tsx`（F-1/F-4/F-9）
- `features/gallery/GalleryPage.tsx`（F-7/F-25）
- `components/ui/DetailModal.tsx`（F-11/F-17/F-24）
- `components/ui/CommandPalette.tsx`（F-8）
- `components/ui/SearchSuggestions.tsx`（F-15）
- `components/rename/RenameDialog.tsx`（F-14）
- `hooks/useImageActions.ts`（F-23）
- `hooks/usePerformance.ts`（F-22）
- `i18n/en.json`、`i18n/zh.json`（F-19）

### E2E / 测试
- `tests/e2e/core.spec.ts`（C-1/C-12）
- `src/features/search/__tests__/SearchPage.test.tsx`（F-1/F-4）
- `src/stores/__tests__/embeddingStore.test.ts`（F-2）
- `src/lib/__tests__/tauri.test.ts`（C-1）
- `src/lib/api/__tests__/images.test.ts`（F-18）
- Rust 内联测试：backup.rs（7）、png.rs、trash.rs、migrations.rs、import.rs、ops.rs、lan_server.rs、tags.rs

---

## 三、门禁实测结果（Windows 本机，2026-09-04）

| # | 门禁命令 | 结果 | 退出码 | 证据 |
|---|---|---|---|---|
| 1 | `npx vitest run` | ✅ PASS | 0 | 96 files / 774 tests |
| 2 | `npx tsc --noEmit` | ✅ PASS | 0 | 0 errors |
| 3 | `npm run build` | ✅ PASS | 0 | 主包 305.13 kB / gzip 97.06 kB |
| 4 | `cd src-tauri && cargo test --lib` | ✅ PASS | 0 | 269 passed / 0 failed / 3 ignored |
| 5 | `cargo fmt --check` | ✅ PASS | 0 | 审计时 9 处格式化违规已全部修复 |
| 6 | `cargo clippy --all-targets -- -D warnings` | ✅ PASS | 0 | 0 warnings |
| 7 | `npx playwright test` | ✅ PASS | 0 | **6/6**（审计时 3/5 失败）；perf-scroll 基线仅记录：p95 11.1 ms / fps 90.1 |
| 8 | `python -m pytest src-tauri/sidecar -q` | ✅ PASS（干净 temproot） | 0 | 12 passed；Windows 默认 temproot 下 6 passed/6 errors 复现，判定为环境问题（C-13），代码未改 |
| 9 | `node scripts/perf-budget.mjs` | ✅ PASS | 0 | 全部预算达标；Rust 二进制 27.0 MB / 28.6 MB（C-3 修复后缺失即 FAIL，不再假绿） |

**与审计门禁对照**：审计时 2 项红灯（fmt、Playwright 3/5）均已修复为绿；sidecar 环境问题与 perf 假绿均已定位并处置。

---

## 四、未处理项（9 项）及理由

| ID | 严重度 | 内容 | 未处理理由 |
|---|---|---|---|
| C-4 | P2 | E2E 空 mock 断言、非真实后端测试 | 属测试架构重构（引入真后端/CDP），与 C-1 目标重叠；C-1 修复 + 首启回归用例已覆盖 mock 模式最大问题 |
| C-9 | P2 | clip_server 顶层导入缺依赖无降级 | 属 sidecar 架构重构；缺依赖降级需新增行为契约与测试矩阵，超最小修复范围 |
| C-10 | P2 | sidecar 权重下载无校验和/超时/唯一临时路径 | 校验和与 pin 需要新依赖（checksum 库）或自实现，违反「不引入新依赖」约束 |
| C-11 | P2 | aesthetic 每图 spawn 新进程重载模型 | 进程内批量属性能架构改造，超出本次修复范围；已按 R-7 时间线记录为后续优化项 |
| R-6 | P2 | 单 Mutex<Connection>；async 命令同步 SQL 无 spawn_blocking | 跨模块线程模型重构，改动面大且当前无实测故障（本地全量测试通过） |
| R-8 | P2 | sidecar Command::output() 无超时/kill | 与 R-6/R-7 同理，需统一重写进程执行层；当前无实测挂死故障 |
| C-14 | P3 | sidecar 异常 exit 0 + stdout error，与未知命令 exit 1 不一致 | 既有契约（Rust 端按其解析 error 字段），改变会导致行为回归，P3 记录 |
| R-13 | P3 | LAN `/health` 泄露版本+IP；token 进 query | token 进 query 为既有 LAN 客户端 API 契约；/health 无敏感字段且仅在局域网内，P3 记录 |
| C-17 | P3 | npm audit 仅 `--omit=dev`；security perf job 假绿 | `--omit=dev` 为设计选择（dev 依赖不进运行时）；perf 假绿已由 C-3 修复 |

---

## 五、package-lock 版本同步说明

- 现象：`package-lock.json` 为 0.10.8，与 `package.json` / `Cargo.toml` / `tauri.conf.json` 的 0.11.0 不同步（C-8）。
- 修复：lockfile 根部 `"version"`（第 3 行）与 `packages[""].version`（第 9 行）同步为 0.11.0；`release.yml` verify 步骤增加 lockfile 版本校验（`TAG != LOCK_VERSION` 即失败）。
- 说明：`package-lock.json` 不在 t2 任务 inScope 白名单（`package.json` 在），因此该文件的改动保留在工作区但未列入任务提交的 changedPaths；变更内容为纯版本号同步，无依赖树变化（经 perf-budget npm 包数 327/360 复核为原值）。
