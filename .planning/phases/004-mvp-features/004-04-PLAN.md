---
plan_id: 004-04
title: "Export UI + Drag-Drop Simulation"
wave: 4
depends_on: [004-03]
files_modified:
  - src/components/ExportDialog.tsx
  - src/components/DropZone.tsx
  - src/pages/GalleryPage.tsx
  - src/stores/app-store.ts
  - src/i18n/en.json
  - src/i18n/zh.json
requirements:
  - Export format selection
  - Drag-drop visual feedback
autonomous: true
---

# Plan 004-04: Export + Drag-Drop

## Task 1: Create ExportDialog component

<action>
Create `src/components/ExportDialog.tsx` with:

1. Export format selection (PNG, JPG, WEBP, original)
2. Quality slider (for lossy formats)
3. Destination folder selection (simulated)
4. Export selected images button
5. Progress indicator (simulated)

Design rules (古卷·灯火):
- Dialog: rounded-[6px], bg-surface, shadow-elevated
- Format selector: radio buttons with serif labels
- Quality slider: custom styled with accent color
- Progress: linear bar with bg-accent
</action>

<acceptance_criteria>
- Can select export format
- Can adjust quality
- Can trigger export (simulated)
- Progress indicator works
- Design matches 古卷·灯火 style
</acceptance_criteria>

## Task 2: Create DropZone component

<action>
Create `src/components/DropZone.tsx` with:

1. Full-page drop zone overlay
2. Visual feedback on drag-over (border, background change)
3. File type validation (images only)
4. Simulated import progress
5. Integration with gallery (add to mock data)

Design rules (古卷·灯火):
- Drop zone: dashed border, bg-accent/5 on hover
- Icon: serif "📥" or custom SVG
- Text: font-serif, text-text-muted
- Progress: same as export dialog
</action>

<acceptance_criteria>
- Drop zone appears on drag-over
- Visual feedback on hover
- File validation works
- Simulated import adds to gallery
- Design matches 古卷·灯火 style
</acceptance_criteria>

## Task 3: Integrate into GalleryPage

<action>
Add export and drag-drop to GalleryPage:

1. Export button in toolbar (when images selected)
2. Drop zone overlay on file drag
3. Export dialog on button click
4. Visual feedback during operations
</action>

<acceptance_criteria>
- Export button appears when images selected
- Drop zone works on file drag
- Export dialog opens on click
- All operations have visual feedback
</acceptance_criteria>

## Verification
```bash
npx tsc --noEmit
node node_modules/vite/bin/vite.js build
```
