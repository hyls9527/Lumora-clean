---
plan_id: 001-02
title: "Command Palette + Keyboard Navigation"
wave: 2
depends_on: [001-01]
files_modified:
  - src/components/CommandPalette.tsx (new)
  - src/components/Sidebar.tsx
  - src/pages/GalleryPage.tsx
  - src/App.tsx
  - src/stores/app-store.ts
  - src/i18n/en.json
  - src/i18n/zh.json
requirements:
  - FEAT-01-01
  - FEAT-01-02
autonomous: true
---

# Plan 001-02: Command Palette + Keyboard Navigation

## Task 1: Create CommandPalette component

<read_first>
- src/components/ui/dialog.tsx (shadcn dialog)
- src/components/ui/input.tsx (shadcn input)
- src/stores/app-store.ts (current state)
</read_first>

<action>
Create src/components/CommandPalette.tsx:

1. Use shadcn Dialog component
2. Search input at top with auto-focus
3. Filtered list of commands:
   - Navigation: Gallery, Curation, Dashboard, Settings, Trash
   - Actions: Select All, Clear Selection, Delete Selected
   - Sort: By Date, By Rating, By Size
4. Keyboard navigation: Up/Down arrows, Enter to execute, Esc to close
5. ⌘K / Ctrl+K global shortcut to open
6. Style: warm white theme, 13px text, subtle borders
7. Results: icon + label + keyboard hint (right-aligned)
</action>

<acceptance_criteria>
- ⌘K / Ctrl+K opens command palette
- Typing filters commands
- Arrow keys navigate, Enter executes, Esc closes
- Commands actually work (navigate views, trigger actions)
- Uses shadcn Dialog component
- Matches DESIGN.md style
</acceptance_criteria>

## Task 2: Add keyboard navigation to Gallery

<read_first>
- src/pages/GalleryPage.tsx
- src/components/ImageCard.tsx
</read_first>

<action>
Add keyboard navigation to GalleryPage:

1. Arrow keys: navigate between images in grid
2. Enter: open detail panel for focused image
3. Space: toggle selection on focused image
4. Delete/Backspace: delete focused image (move to trash)
5. Escape: clear selection / close detail panel
6. Show focus ring on currently focused image card
7. Track focused index in app-store
</action>

<acceptance_criteria>
- Arrow keys move focus between image cards
- Enter opens detail panel
- Space toggles selection
- Delete moves to trash
- Focus ring visible on current card
</acceptance_criteria>

## Task 3: Wire up App.tsx + i18n

<read_first>
- src/App.tsx
- src/i18n/en.json
- src/i18n/zh.json
</read_first>

<action>
1. Import and render CommandPalette in App.tsx
2. Add i18n keys for command palette labels
3. Add i18n keys for keyboard shortcut hints
4. Ensure CommandPalette renders above all other content
</action>

<acceptance_criteria>
- CommandPalette visible in app
- All text uses i18n keys
- No raw English/Chinese strings in components
</acceptance_criteria>

## Verification

After all changes:
```bash
npx tsc --noEmit
node node_modules/vite/bin/vite.js build
```

## must_haves
- Zero TypeScript errors
- Clean build
- Command palette fully functional
- Keyboard navigation works in gallery
- All text internationalized
