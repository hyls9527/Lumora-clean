---
phase: 001-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/DetailPanel.tsx
  - src/components/ImageCard.tsx
  - src/components/ui/plum-flower.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/input.tsx
  - src/components/ui/scroll-area.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/tabs.tsx
  - src/components/ui/tooltip.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11

must_haves:
  truths:
    - "DetailPanel action buttons display only Chinese text labels (关闭, 复制路径, 删除, 标签, 全屏) — zero lucide-react icon elements"
    - "ImageCard hover effect uses shadow deepening only (shadow-card → shadow-card-hover) — zero hover:scale or hover:scale-[*] classes"
    - "ImageCard rating and bookmark toggle buttons use background-only hover (hover:bg-surface-hover) — zero hover:scale classes on any button"
    - "PlumFlower SVG renders from one shared component at src/components/ui/plum-flower.tsx — no independent PlumFlower definitions exist in DetailPanel.tsx or ImageCard.tsx"
    - "All 12 UI primitives use exclusively DESIGN.md color tokens (bg-surface, bg-bg, text-text, text-text-muted, bg-danger, border-border) — zero shadcn generic tokens (bg-primary, bg-secondary, bg-background, bg-card, bg-muted, bg-popover, text-primary-foreground, text-foreground, text-muted-foreground, ring-ring, ring-offset-background, border-input)"
    - "All 12 UI primitives use correct border radii per DESIGN.md material logic: cards/skeleton=2px (rounded-sm), buttons/inputs/badges/tabs=4px (rounded-md), dialogs/dropdowns/tooltips/sheets=6px (rounded-lg)"
    - "All 12 UI primitives use DESIGN.md 3-layer shadow system: shadow-card for cards, shadow-elevated for overlays/dialogs/dropdowns/tooltips"
    - "All 12 UI primitives use 200ms ease-out transitions — zero transition-colors, zero duration-300, zero duration-500"
  artifacts:
    - path: "src/components/ui/plum-flower.tsx"
      provides: "Shared PlumFlower SVG rating component"
      exports: ["PlumFlower"]
      contains: "PlumFlowerProps"
    - path: "src/components/DetailPanel.tsx"
      provides: "Image detail panel — no lucide-react imports, uses shared PlumFlower"
    - path: "src/components/ImageCard.tsx"
      provides: "Gallery image card — no hover:scale, uses shared PlumFlower"
  key_links:
    - from: "src/components/DetailPanel.tsx"
      to: "src/components/ui/plum-flower.tsx"
      via: "named import"
      pattern: "import.*PlumFlower.*from.*plum-flower"
    - from: "src/components/ImageCard.tsx"
      to: "src/components/ui/plum-flower.tsx"
      via: "named import"
      pattern: "import.*PlumFlower.*from.*plum-flower"
    - from: "src/components/ui/badge.tsx"
      to: "src/index.css @theme block"
      via: "DESIGN.md token mapping"
      pattern: "bg-surface.*text-text|bg-danger.*text-surface"
    - from: "src/components/ui/button.tsx"
      to: "src/index.css @theme block"
      via: "DESIGN.md token mapping"
      pattern: "bg-text.*text-surface|bg-surface.*text-text|bg-danger.*text-surface"
    - from: "src/components/ui/dialog.tsx"
      to: "src/index.css @theme block"
      via: "shadow-elevated token + rounded-lg"
      pattern: "shadow-elevated.*rounded-lg"
---

<objective>
Eliminate the two highest-priority anti-patterns (lucide-react icons in DetailPanel, hover:scale in ImageCard), extract the duplicated PlumFlower SVG into a shared UI primitive, and audit all 12 shadcn/ui primitives to use DESIGN.md tokens exclusively.

Purpose: These are the most visible DESIGN.md violations. DetailPanel's lucide icons break the "no icon library" rule. ImageCard's hover:scale breaks the "no image hover scale" rule. Both files have independent PlumFlower copies that have already diverged (ImageCard variant has an extra opacity circle). The UI primitives carry shadcn's default Tailwind token classes that do not resolve in this project's DESIGN.md-only @theme block, leaving elements unstyled.

Output: Three targeted files fully aligned (DetailPanel.tsx, ImageCard.tsx, plum-flower.tsx). Up to 12 UI primitives corrected for token usage, radii, shadows, and transitions per the D-08 audit criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@DESIGN.md
@CLAUDE.md
@.planning/phases/001-ui-polish/001-CONTEXT.md
@.planning/phases/001-ui-polish/001-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@src/index.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create shared PlumFlower component + replace lucide-react icons in DetailPanel (D-05, D-07)</name>
  <files>src/components/ui/plum-flower.tsx, src/components/DetailPanel.tsx</files>

  <read_first>
    - src/components/DetailPanel.tsx — current state with lucide-react imports, inline PlumFlower, and ActionButton pattern
    - src/components/ImageCard.tsx — PlumFlower copy variant (has extra opacity circle on line 25) for reference
    - DESIGN.md — color tokens (--color-accent: #7a5c12), anti-patterns list
    - src/index.css — @theme block verifying --color-accent and --color-accent-hover are defined
    - .planning/phases/001-ui-polish/001-UI-SPEC.md — PlumFlower component contract (PlumFlowerProps interface, SVG geometry)
    - .planning/codebase/CONVENTIONS.md — named export pattern, sub-component pattern, interface naming
  </read_first>

  <action>
    **Part A — Create src/components/ui/plum-flower.tsx (per D-07):**

    Create a new file at `src/components/ui/plum-flower.tsx`. Export `PlumFlower` as a named function component with this interface:
    - `interface PlumFlowerProps { filled: boolean; size?: number }` — default size 18 (used for both width and height on the SVG)
    - SVG geometry: 5-petal plum flower using ellipses rotated at 0/72/144/216/288 degrees around center (9,9), plus a center circle. Petal rx=3, ry=4.5, center circle r=2.
    - When `filled=true`: petal fill=`var(--color-accent)`, no stroke. Center circle fill=`var(--color-accent)`.
    - When `filled=false`: petal fill=`none`, stroke=`rgba(139,115,75,0.2)` width=1. Center circle fill=`rgba(139,115,75,0.12)`.
    - SVG `viewBox="0 0 18 18"`, width and height set from `size` prop.
    - Use the DetailPanel.tsx inline PlumFlower as the geometry source (lines 14-31) — NOT the ImageCard.tsx variant which adds an extra opacity circle on line 25.

    **Part B — Wire DetailPanel.tsx to shared PlumFlower (per D-07):**

    - Add import: `import { PlumFlower } from "@/components/ui/plum-flower"`
    - Delete the inline `function PlumFlower({ filled }: { filled: boolean })` definition (lines 14-31)
    - The rating buttons already pass `filled={star <= image.rating}` — no prop changes needed since the shared component has the same `filled: boolean` prop.
    - Pass `size={18}` explicitly for clarity, or rely on the default value — both produce identical output.

    **Part C — Replace lucide-react icons with text labels (per D-05):**

    Remove the lucide-react import block (lines 4-10: `X, Copy, Trash2, Tag, Maximize2`).

    Replace each icon usage with plain Chinese text:

    1. **Close button (line 50):** Replace `<X className="w-4 h-4 text-text-muted" />` with `<span className="text-[12px] text-text-muted">关闭</span>`. Keep the parent button styling (`w-7 h-7 rounded-[4px] flex items-center justify-center hover:bg-surface-hover transition-all duration-200 ease-out`).

    2. **Maximize/overlay button (line 63-64):** Replace `<Maximize2 className="w-3.5 h-3.5" />` with `<span className="text-[11px]">全屏</span>` inside the existing absolute-positioned button.

    3. **ActionButton sub-component (lines 254-276):** Refactor the helper to use text labels instead of lucide-react icons:
    - Change signature from `{ icon: React.ElementType; label: string; variant?: "danger" }` to `{ label: string; description: string; variant?: "danger" }` — where `label` is the Chinese prefix text ("复制路径", "标签", "删除") and `description` is the i18n translated string.
    - Replace `<Icon className="w-3.5 h-3.5" />` with `<span className="text-[12px] w-[18px] text-center shrink-0">{label}</span>`
    - Rename old `<span>{label}</span>` to `<span>{description}</span>`
    - Three call sites (lines 190-196) become:
      - `<ActionButton label="复制路径" description={t("detail.copyPath")} />`
      - `<ActionButton label="标签" description={t("detail.addTag")} />`
      - `<ActionButton label="删除" description={t("detail.delete")} variant="danger" />`

    **Cleanup after all changes:**
    - Confirm the `Badge` import (line 11) is still used (it is, line 129 for tags).
    - Run `npx tsc --noEmit` to catch any type errors from the ActionButton refactor.
  </action>

  <verify>
    <automated>grep -c "lucide-react" src/components/DetailPanel.tsx</automated>
    <automated>grep -c "from \"lucide-react\"" src/components/DetailPanel.tsx</automated>
    <automated>grep -c "function PlumFlower" src/components/DetailPanel.tsx</automated>
    <automated>grep -c "import.*PlumFlower.*from.*plum-flower" src/components/DetailPanel.tsx</automated>
    <automated>grep -c "export function PlumFlower" src/components/ui/plum-flower.tsx</automated>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -c "lucide-react" src/components/DetailPanel.tsx` returns 0
    - Source assertion: `grep -c "from \"lucide-react\"" src/components/DetailPanel.tsx` returns 0
    - Source assertion: `grep -c "function PlumFlower" src/components/DetailPanel.tsx` returns 0
    - Source assertion: `grep -c "from \"@/components/ui/plum-flower\"" src/components/DetailPanel.tsx` returns 1
    - Source assertion: `grep -c "export function PlumFlower" src/components/ui/plum-flower.tsx` returns 1
    - Source assertion: `grep "interface PlumFlowerProps" src/components/ui/plum-flower.tsx` shows `filled: boolean` and `size?: number`
    - Source assertion: DetailPanel ActionButton calls use string labels "复制路径", "标签", "删除" — not React.ElementType icon references
    - Behavior: `npx tsc --noEmit` exits with zero TypeScript errors (pre-existing errors are OK but zero NEW errors in DetailPanel.tsx or plum-flower.tsx)
  </acceptance_criteria>

  <done>No lucide-react icons remain in DetailPanel.tsx. PlumFlower SVG lives in one shared file imported by DetailPanel.tsx. ActionButton uses text labels. TypeScript compiles cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire ImageCard to shared PlumFlower + remove all hover:scale transforms (D-06, D-07)</name>
  <files>src/components/ImageCard.tsx</files>

  <read_first>
    - src/components/ImageCard.tsx — current state with inline PlumFlower, hover:scale classes, hover:scale-110, hover:scale-125, duration-150
    - src/components/ui/plum-flower.tsx — the shared PlumFlower created by Task 1
    - DESIGN.md — anti-pattern "图片 hover scale", shadow system (shadow-card → shadow-card-hover)
    - .planning/phases/001-ui-polish/001-UI-SPEC.md — D-06 contract (exact lines to remove, replacement behavior)
    - .planning/codebase/CONVENTIONS.md — transition pattern: 200ms ease-out
  </read_first>

  <action>
    **Part A — Wire shared PlumFlower (per D-07):**

    - Add import: `import { PlumFlower } from "@/components/ui/plum-flower"`
    - Delete the inline `function PlumFlower` definition (lines 6-29)
    - In the rating button (line 143): `<PlumFlower filled={star <= image.rating} />` — keep the same prop, add explicit `size={14}` since the card uses 14x14 rating buttons

    **Part B — Remove hover:scale from card wrapper (per D-06):**

    - Line 48: Remove `"hover:scale-[1.02]",` from the className array on the card wrapper div (lines 43-52)
    - Line 46: Change `"shadow-sm hover:shadow-card-hover"` to `"shadow-card hover:shadow-card-hover"` — the card default shadow must use `shadow-card` (not `shadow-sm` which is a weaker token)
    - The card wrapper already has `"transition-all duration-200 ease-out"` — keep this

    **Part C — Remove hover:scale from favorite stamp button (per D-06):**

    - Line 102: Remove `"hover:scale-110 hover:shadow-md active:scale-90"` from the favorite stamp div className
    - Replace with `"hover:bg-accent-hover"` — simple background change on hover, matching DESIGN.md button hover behavior
    - The div already has `"transition-all duration-200 ease-out"` — keep this
    - Line 117-119: Remove the `image.favorite && "scale-110"` from the ◆ span — replace with no extra transform. The accent background change on the parent div already signals favorited state.

    **Part D — Remove hover:scale from rating buttons (per D-06):**

    - Line 137: Change `className="w-[14px] h-[14px] flex items-center justify-center hover:scale-125 transition-transform duration-150 ease-out"` to `className="w-[14px] h-[14px] flex items-center justify-center hover:bg-surface-hover rounded-[2px] transition-all duration-200 ease-out"`
    - Key changes: `hover:scale-125` removed, `duration-150` → `duration-200`, `transition-transform` → `transition-all`, added `hover:bg-surface-hover rounded-[2px]` for visible hover feedback without scale

    **Consistency check after all changes:**
    - Verify no `scale` substring remains in the file (for transform purposes)
    - Verify all transitions use `duration-200`
  </action>

  <verify>
    <automated>grep -c "hover:scale" src/components/ImageCard.tsx</automated>
    <automated>grep -c "scale-\[" src/components/ImageCard.tsx</automated>
    <automated>grep -c "scale-110\|scale-125" src/components/ImageCard.tsx</automated>
    <automated>grep -c "duration-150" src/components/ImageCard.tsx</automated>
    <automated>grep -c "function PlumFlower" src/components/ImageCard.tsx</automated>
    <automated>grep -c "from \"@/components/ui/plum-flower\"" src/components/ImageCard.tsx</automated>
    <automated>grep -c "shadow-card hover:shadow-card-hover" src/components/ImageCard.tsx</automated>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -c "hover:scale" src/components/ImageCard.tsx` returns 0
    - Source assertion: `grep -c "scale-110" src/components/ImageCard.tsx` returns 0; `grep -c "scale-125" src/components/ImageCard.tsx` returns 0
    - Source assertion: `grep -c "duration-150" src/components/ImageCard.tsx` returns 0
    - Source assertion: `grep -c "function PlumFlower" src/components/ImageCard.tsx` returns 0
    - Source assertion: `grep -c "from \"@/components/ui/plum-flower\"" src/components/ImageCard.tsx` returns 1
    - Source assertion: `grep "shadow-card hover:shadow-card-hover" src/components/ImageCard.tsx` matches on the card wrapper className
    - Source assertion: Rating button className contains `duration-200` and `hover:bg-surface-hover` — no `duration-150`, no `scale`
    - Source assertion: Favorite stamp className contains `hover:bg-accent-hover` — no `scale`, no `shadow-md`
    - Behavior: `npx tsc --noEmit` exits with zero TypeScript errors (no new errors in ImageCard.tsx)
  </acceptance_criteria>

  <done>ImageCard hover triggers shadow-deepening only. No scale transforms anywhere in the file. PlumFlower imported from shared component. All transitions are 200ms ease-out.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Audit and fix 12 UI primitives for DESIGN.md token alignment (D-01, D-02, D-04, D-08, D-09, D-10, D-11)</name>
  <files>src/components/ui/badge.tsx, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/dialog.tsx, src/components/ui/dropdown-menu.tsx, src/components/ui/input.tsx, src/components/ui/scroll-area.tsx, src/components/ui/separator.tsx, src/components/ui/sheet.tsx, src/components/ui/skeleton.tsx, src/components/ui/tabs.tsx, src/components/ui/tooltip.tsx</files>

  <read_first>
    - src/index.css — authoritative @theme block defining all DESIGN.md tokens (--color-*, --shadow-*, --radius-*, --font-*)
    - DESIGN.md — border radius material logic (cards=2px, buttons=4px, inputs=4px, tags=4px, dialogs=6px), shadow system (shadow-card, shadow-card-hover, shadow-elevated), transition specification (200ms ease-out), anti-patterns
    - src/components/ui/badge.tsx — current state with shadcn default tokens
    - src/components/ui/button.tsx — current state with shadcn default tokens + CVA variants
    - src/components/ui/card.tsx — current state with shadcn default tokens + compound pattern
    - src/components/ui/dialog.tsx — current state, overlay and content tokens
    - src/components/ui/dropdown-menu.tsx — current state, popover tokens
    - src/components/ui/input.tsx — current state, input tokens
    - src/components/ui/scroll-area.tsx — current state
    - src/components/ui/separator.tsx — current state
    - src/components/ui/sheet.tsx — current state, overlay and panel tokens, non-200ms transitions
    - src/components/ui/skeleton.tsx — current state, muted token
    - src/components/ui/tabs.tsx — current state, muted/background tokens
    - src/components/ui/tooltip.tsx — current state, popover tokens
    - .planning/phases/001-ui-polish/001-UI-SPEC.md — D-10 rule (keep as-is with minimal tweaks), audit criteria (6 checks)
  </read_first>

  <action>
    **Audit approach per D-10:** Read each file. Check against the 6 audit criteria. Fix ONLY specific DESIGN.md deviations. Do NOT rewrite component structure, do NOT change component behavior, do NOT alter the CVA variant system.

    **Audit Criteria (from UI-SPEC):**
    1. Colors use DESIGN.md CSS custom properties (no hardcoded hex, no shadcn generic tokens)
    2. Font families match DESIGN.md roles
    3. Spacing values from DESIGN.md scale only
    4. Border radii match DESIGN.md material logic
    5. Shadows use 3-layer system, never custom box-shadows
    6. Transitions are 200ms ease-out, never 100ms, never spring/bounce

    **Fix only the specific shadcn→DESIGN.md token mismatches below. Leave everything else untouched.**

    ---

    **SHADCN→DESIGN.md Token Replacement Rules (apply EXACTLY as listed):**

    Color class replacements:
    - `bg-background` → `bg-surface`
    - `bg-card` → `bg-surface`
    - `bg-popover` → `bg-surface`
    - `text-foreground` → `text-text`
    - `text-card-foreground` → `text-text`
    - `text-popover-foreground` → `text-text`
    - `text-muted-foreground` → `text-text-muted`
    - `bg-muted` / `bg-muted/50` → `bg-bg`
    - `bg-primary text-primary-foreground` → `bg-text text-surface`
    - `bg-secondary text-secondary-foreground` → `bg-surface text-text`
    - `bg-destructive text-destructive-foreground` → `bg-danger text-surface`
    - `border-input` → `border-border`
    - `bg-black/80` (dialog/sheet overlays) → `bg-text/80`
    - `shadow-md` / `shadow-lg` → `shadow-elevated`
    - `shadow-sm` on non-card elements → `shadow-card`
    - `ring-ring` → `ring-accent/30`
    - `ring-offset-background` → `ring-offset-surface`
    - `data-[state=open]:bg-accent` (separate from DESIGN.md accent token usage) → `data-[state=open]:bg-surface-hover`
    - `hover:bg-accent hover:text-accent-foreground` → `hover:bg-surface-hover hover:text-text`
    - `focus:bg-accent focus:text-accent-foreground` → `focus:bg-surface-hover focus:text-text`

    Transition replacements:
    - `transition-colors` → `transition-all duration-200 ease-out`
    - `duration-300` → `duration-200`
    - `duration-500` → `duration-200`
    - `ease-in-out` → `ease-out`
    - `animate-in` / `animate-out`: keep animation classes but ensure `duration-200` is present alongside

    Radius rules (per DESIGN.md material logic):
    - Cards, skeleton → 2px: change `rounded-xl` to `rounded-sm`
    - Buttons, inputs, badges, tabs → 4px: keep `rounded-md`, change `rounded-sm` to `rounded-[4px]` if used on these elements
    - Dialogs, dropdowns, tooltips, sheets → 6px: change `rounded-md` to `rounded-lg` on these elements
    - CardHeader `rounded-t-xl` → `rounded-t-sm`; CardFooter `rounded-b-xl` → `rounded-b-sm`

    ---

    **PER-FILE instructions:**

    **badge.tsx:** replaced `rounded-md`=correct. Replace `bg-primary text-primary-foreground`→`bg-text text-surface`, `bg-secondary text-secondary-foreground`→`bg-surface text-text`, `bg-destructive text-destructive-foreground`→`bg-danger text-surface`. Replace `transition-colors`→`transition-all duration-200 ease-out`. Replace `focus:ring-ring ring-offset-2`→`focus:ring-accent/30 ring-offset-2`.

    **button.tsx:** `rounded-md`=correct. Replace variant tokens: default `bg-primary text-primary-foreground hover:bg-primary/90`→`bg-text text-surface hover:bg-text/90`, destructive `bg-destructive text-destructive-foreground hover:bg-destructive/90`→`bg-danger text-surface hover:bg-danger/90`, outline `border-input bg-background hover:bg-accent hover:text-accent-foreground`→`border-border bg-surface hover:bg-surface-hover hover:text-text`, secondary `bg-secondary text-secondary-foreground hover:bg-secondary/80`→`bg-surface text-text hover:bg-surface-hover`, ghost `hover:bg-accent hover:text-accent-foreground`→`hover:bg-surface-hover hover:text-text`, link `text-primary`→`text-accent`. Replace `transition-colors`→`transition-all duration-200 ease-out`. Replace `ring-offset-background focus-visible:ring-ring`→`ring-offset-surface focus-visible:ring-accent/30`.

    **card.tsx:** Replace `rounded-xl`→`rounded-sm`. CardHeader `rounded-t-xl`→`rounded-t-sm`. CardFooter `rounded-b-xl`→`rounded-b-sm`. Replace `bg-card text-card-foreground`→`bg-surface text-text`. Replace `ring-foreground/10`→`ring-border`. Replace `bg-muted/50`→`bg-bg`.

    **dialog.tsx:** Overlay `bg-black/80`→`bg-text/80`. Content `bg-background`→`bg-surface`. `shadow-lg`→`shadow-elevated`. Content `rounded-lg`=correct. Close button `rounded-sm`→`rounded-[4px]`. Replace `data-[state=open]:bg-accent data-[state=open]:text-muted-foreground`→`data-[state=open]:bg-surface-hover data-[state=open]:text-text-muted`.

    **dropdown-menu.tsx:** Content `rounded-md`→`rounded-lg`. Replace `bg-popover text-popover-foreground`→`bg-surface text-text`. `shadow-md`→`shadow-elevated`. MenuItem `hover:bg-accent hover:text-accent-foreground`→`hover:bg-surface-hover hover:text-text`. `focus:bg-accent focus:text-accent-foreground`→`focus:bg-surface-hover focus:text-text`. MenuItem `rounded-sm`→`rounded-[4px]`. `transition-colors`→`transition-all duration-200 ease-out`. Separator `bg-muted`→`bg-border`. Label `font-semibold`→`font-medium`.

    **input.tsx:** `rounded-md`=correct. Replace `border-input bg-background`→`border-border bg-surface`. Replace `placeholder:text-muted-foreground`→`placeholder:text-text-muted`. Replace `ring-ring ring-offset-background`→`ring-accent/30 ring-offset-surface`.

    **scroll-area.tsx:** Uses only `overflow-auto` and `hidden` — NO shadcn tokens. ALREADY CORRECT. No changes needed.

    **separator.tsx:** Uses `bg-border` which resolves to DESIGN.md `--color-border`. ALREADY CORRECT. No changes needed.

    **sheet.tsx:** Overlay `bg-black/80`→`bg-text/80`. Content `bg-background`→`bg-surface`. `shadow-lg`→`shadow-elevated`. Close button `rounded-sm`→`rounded-[4px]`. Replace `bg-secondary`→`bg-surface-hover`. SheetTitle `text-foreground`→`text-text`. SheetDescription `text-muted-foreground`→`text-text-muted`. Replace `ring-ring ring-offset-background`→`ring-accent/30 ring-offset-surface`. **Critical:** Replace `data-[state=closed]:duration-300 data-[state=open]:duration-500`→`data-[state=closed]:duration-200 data-[state=open]:duration-200`. Replace `ease-in-out`→`ease-out`.

    **skeleton.tsx:** `rounded-md`→`rounded-sm` (skeleton is card placeholder, 2px). Replace `bg-muted`→`bg-bg`.

    **tabs.tsx:** TabsList `rounded-md`→`rounded-[4px]`. Replace `bg-muted text-muted-foreground`→`bg-bg text-text-muted`. TabsTrigger `rounded-sm`→`rounded-[4px]`. Replace `ring-offset-background ring-ring`→`ring-offset-surface ring-accent/30`. Replace `data-[state=active]:bg-background data-[state=active]:text-foreground data-[state=active]:shadow-sm`→`data-[state=active]:bg-surface data-[state=active]:text-text data-[state=active]:shadow-card`. TabsContent replace `ring-offset-background ring-ring`→`ring-offset-surface ring-accent/30`. Replace `transition-all`→`transition-all duration-200 ease-out`.

    **tooltip.tsx:** Content `rounded-md`→`rounded-lg` (tooltip overlay, 6px). Replace `bg-popover text-popover-foreground`→`bg-surface text-text`. `shadow-md`→`shadow-elevated`.

    ---

    **Post-audit verification:** Run `npx tsc --noEmit` to confirm zero new TypeScript errors. Run `npm run build` to confirm Vite bundles successfully.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: Zero shadcn generic tokens across all 12 primitives — run `grep -rn "bg-background\|bg-card\|bg-popover\|bg-muted\|text-foreground\|text-card-foreground\|text-popover-foreground\|text-muted-foreground\|ring-ring\|ring-offset-background\|border-input\|bg-primary\|bg-secondary\|bg-destructive\|text-primary-foreground\|text-secondary-foreground\|text-destructive-foreground" src/components/ui/ --include="*.tsx"` and expect zero matches (confirm none are in comments)
    - Source assertion: Zero wrong-duration transitions — `grep -rn "duration-300\|duration-500\|transition-colors" src/components/ui/ --include="*.tsx"` returns zero matches (except in comments)
    - Source assertion: Card uses `rounded-sm` not `rounded-xl`
    - Source assertion: Dialog overlay content uses `rounded-lg`, close button uses `rounded-[4px]`
    - Source assertion: DropdownMenuContent and TooltipContent use `rounded-lg` not `rounded-md`
    - Source assertion: `shadow-md` and `shadow-lg` replaced with `shadow-elevated` in dialog, dropdown-menu, tooltip, sheet
    - Source assertion: Overlays use `bg-text/80` not `bg-black/80`
    - Source assertion: Sheet transitions use `duration-200` not `duration-300`/`duration-500`
    - Behavior: `npx tsc --noEmit` exits with zero TypeScript errors
    - Behavior: `npm run build` succeeds
  </acceptance_criteria>

  <done>All 12 UI primitives use DESIGN.md tokens exclusively. No shadcn generic tokens remain. All radii, shadows, and transitions match DESIGN.md spec. TypeScript compiles and Vite builds successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system | Reading/writing source files — no user data involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-001-01 | Tampering | npm dependencies | accept | No new packages installed — only code modifications to existing source files. Package legitimacy not applicable. |
| T-001-02 | Information Disclosure | N/A | accept | Pure UI polish — no user data, no API, no backend. Zero information disclosure surface. |
</threat_model>

<verification>
Overall phase checks after ALL tasks complete:
1. `grep -c "lucide-react" src/components/DetailPanel.tsx` returns 0
2. `grep -c "hover:scale" src/components/ImageCard.tsx` returns 0
3. `grep -c "function PlumFlower" src/components/DetailPanel.tsx src/components/ImageCard.tsx` returns 0 in both files
4. `grep -c "export function PlumFlower" src/components/ui/plum-flower.tsx` returns 1
5. `npx tsc --noEmit` passes cleanly
6. `npm run build` succeeds
</verification>

<success_criteria>
- [ ] DetailPanel.tsx: zero lucide-react imports, ActionButton uses text labels, imports PlumFlower from shared component
- [ ] ImageCard.tsx: zero hover:scale classes, zero duration-150, shadow-card→shadow-card-hover on hover, imports PlumFlower from shared component
- [ ] plum-flower.tsx: exports PlumFlower with PlumFlowerProps { filled, size? }, SVG renders 5-petal flower
- [ ] All 12 UI primitives: zero shadcn generic tokens, correct radii, correct shadows, 200ms ease-out transitions
- [ ] TypeScript compiles with zero new errors
- [ ] Vite build succeeds
</success_criteria>

<output>
Create `.planning/phases/001-ui-polish/001-01-SUMMARY.md` when done
</output>
