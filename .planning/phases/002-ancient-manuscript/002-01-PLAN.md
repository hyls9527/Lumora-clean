---
plan_id: 002-01
title: "Foundation — index.css theme + DESIGN.md rewrite"
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - DESIGN.md
requirements:
  - All color/shadow/transition tokens
autonomous: true
---

# Plan 002-01: Foundation

## Task 1: Rewrite index.css @theme block

<read_first>
- src/index.css
</read_first>

<action>
Replace the ENTIRE @theme block and global styles with the 古卷·灯火 design system.

### @theme block — replace ALL tokens:

```css
@theme {
  /* === COLOR: 象牙纸页 + 研磨墨 + 古铜包浆 === */
  --color-bg: #f2ede4;
  --color-bg-alt: #ebe5d8;
  --color-surface: #f7f2ea;
  --color-surface-hover: #f0ebe0;
  --color-surface-elevated: #f7f2ea;
  --color-border: rgba(139, 115, 75, 0.10);
  --color-border-subtle: rgba(139, 115, 75, 0.05);
  --color-text: #2a2118;
  --color-text-secondary: #6b5d48;
  --color-text-muted: #a09480;
  --color-text-faint: #c4b89e;
  --color-accent: #8b6914;
  --color-accent-hover: #a07818;
  --color-accent-subtle: rgba(139, 105, 20, 0.06);
  --color-accent-muted: #c4a04a;
  --color-danger: #8b3030;
  --color-success: #4a7a3a;

  /* === SHADOW: 灯光投影，暖棕调 === */
  --shadow-xs: 0 1px 2px rgba(78,50,23,0.04);
  --shadow-sm:
    rgba(139,115,75,0.08) 0px 0px 0px 1px,
    rgba(78,50,23,0.03) 0px 1px 2px;
  --shadow-card:
    rgba(139,115,75,0.08) 0px 0px 0px 1px,
    rgba(78,50,23,0.04) 0px 1px 3px;
  --shadow-card-hover:
    rgba(139,115,75,0.14) 0px 0px 0px 1px,
    rgba(78,50,23,0.08) 0px 4px 16px,
    rgba(78,50,23,0.04) 0px 1px 4px;
  --shadow-elevated:
    rgba(139,115,75,0.12) 0px 0px 0px 1px,
    rgba(78,50,23,0.12) 0px 8px 32px,
    rgba(78,50,23,0.06) 0px 2px 8px;

  /* === RADIUS: 更古典 === */
  --radius-sm: 4px;
  --radius-md: 6px;
  --radius-lg: 8px;
  --radius-xl: 12px;
  --radius-button: 9999px;
}
```

### Global styles — update body, transitions, scrollbar:

```css
body {
  background-color: var(--color-bg);
  color: var(--color-text);
  font-family: 'DM Sans', system-ui, -apple-system, sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  font-size: 14px;
  line-height: 1.6;
}

code, .font-mono {
  font-family: 'JetBrains Mono', ui-monospace, monospace;
}

.font-serif {
  font-family: 'Noto Serif SC', 'Georgia', serif;
}

/* Scrollbar — whisper thin */
::-webkit-scrollbar { width: 4px; height: 4px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: rgba(139,115,75,0.12); border-radius: 2px; }
::-webkit-scrollbar-thumb:hover { background: rgba(139,115,75,0.2); }

/* Selection — gold on paper */
::selection { background: var(--color-accent); color: var(--color-surface); }

/* Focus — gold ring */
button:focus-visible, input:focus-visible, a:focus-visible {
  outline: 2px solid var(--color-accent);
  outline-offset: 2px;
  border-radius: 2px;
}

/* Transitions — 200ms, not 100ms */
button, input, a, select, [role="button"] {
  transition: all 200ms ease-out;
}

/* Reduced motion */
@media (prefers-reduced-motion: reduce) {
  * { animation-duration: 0.01ms !important; transition-duration: 0.01ms !important; }
}

/* Paper texture overlay */
body::before {
  content: '';
  position: fixed;
  inset: 0;
  background-image: url("data:image/svg+xml,%3Csvg width='400' height='400' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.75' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='400' height='400' filter='url(%23n)' opacity='0.02'/%3E%3C/svg%3E");
  pointer-events: none;
  z-index: 9999;
}
```

Remove the old Inter font import from index.html — it's banned.
</action>

<acceptance_criteria>
- All CSS variables match the values above exactly
- No Inter font reference anywhere
- body uses DM Sans, .font-serif uses Noto Serif SC
- Transitions are 200ms ease-out (not 150ms or 100ms)
- Paper texture overlay exists on body::before
- Scrollbar is 4px wide with warm brown thumb
</acceptance_criteria>

## Task 2: Rewrite DESIGN.md

<read_first>
- DESIGN.md
</read_first>

<action>
Rewrite DESIGN.md to match the 古卷·灯火 design language. Use the values from the lumora-ui-gsd skill as the source of truth. Key changes:
- Color palette: ivory/ink/patina-gold
- Typography: Noto Serif SC + DM Sans
- Shadows: warm brown tones
- Transitions: 200ms ease-out
- Card radius: 6px
- Rating: dots not stars
- Favorite: stamp not heart
- Add "古卷·灯火" design philosophy section
</action>

<acceptance_criteria>
- DESIGN.md reflects the actual implementation
- No references to old warm-white palette
- No references to Inter font
- No references to 100ms transitions
</acceptance_criteria>

## Verification
```bash
npx tsc --noEmit
node node_modules/vite/bin/vite.js build
```
