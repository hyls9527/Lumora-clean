---
phase: 006-ai-analysis-panel
plan: 03
type: execute
wave: 3
depends_on: [006-01, 006-02]
files_modified:
  - src/components/AiAnalysisSection.tsx
  - src/components/DetailPanel.tsx
autonomous: true
requirements: [AI-01, AI-04]

must_haves:
  truths:
    - "DetailPanel shows AI Analysis section between Tags and existing Analysis section, with a separator above and below"
    - "When no analysis exists for the selected image, the section shows section label + empty state body text (14px text-muted) + 'Analyze with AI' CTA button (12px, accent bg, 4px radius)"
    - "'Analyze with AI' button triggers `triggerAnalysis(imageId)`, shows progress bar (4px accent fill, 2px radius) with 'Analyzing...' text (12px text-muted), and on completion fades in results with 200ms ease-out"
    - "On completion, description (14px text-secondary prose), tag suggestions, content analysis (objects + palette + composition), and history render. CTA transforms to 'Re-analyze' text link"
    - "When triggerAnalysis fails, section shows error text 'Analysis unavailable — try again' in text-danger + 'Retry' text link; CTA resets to idle"
  artifacts:
    - path: "src/components/AiAnalysisSection.tsx"
      provides: "Container component orchestrating all AI analysis sub-sections — description, tags, content, trigger, history"
      exports: ["AiAnalysisSection"]
    - path: "src/components/DetailPanel.tsx"
      provides: "DetailPanel modified to insert AiAnalysisSection after Tags, before existing Analysis section"
      exports: ["DetailPanel"]
  key_links:
    - from: "src/components/AiAnalysisSection.tsx"
      to: "src/stores/ai-analysis-store.ts"
      via: "useAiAnalysisStore"
      pattern: "import.*useAiAnalysisStore"
    - from: "src/components/AiAnalysisSection.tsx"
      to: "src/components/TagSuggestionCard.tsx"
      via: "import TagSuggestionCard"
      pattern: "import.*TagSuggestionCard"
    - from: "src/components/AiAnalysisSection.tsx"
      to: "src/components/ColorPaletteStrip.tsx"
      via: "import ColorPaletteStrip"
      pattern: "import.*ColorPaletteStrip"
    - from: "src/components/AiAnalysisSection.tsx"
      to: "src/components/AnalysisHistoryList.tsx"
      via: "import AnalysisHistoryList"
      pattern: "import.*AnalysisHistoryList"
    - from: "src/components/DetailPanel.tsx"
      to: "src/components/AiAnalysisSection.tsx"
      via: "import AiAnalysisSection, rendered as <AiAnalysisSection imageId={image.id} />"
      pattern: "import.*AiAnalysisSection"
---

<objective>
Build the AiAnalysisSection container component that orchestrates all AI analysis sub-sections (description, tag suggestions, content analysis, trigger CTA, history) based on per-image analysis state from `useAiAnalysisStore`. Integrate it into DetailPanel between the Tags section and the existing Analysis section, completing the AI-01 and AI-04 requirements.

Purpose: This is the final integration plan. Plan 01 established the data layer, Plan 02 built the leaf components — now Plan 03 composes them into the DetailPanel experience. Users will see a complete AI analysis workflow: CTA trigger → progress feedback → results display (description + tags + content + history).

Output: `src/components/AiAnalysisSection.tsx` (new), `src/components/DetailPanel.tsx` (modified)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/006-ai-analysis-panel/006-UI-SPEC.md
@.planning/ROADMAP.md
@src/stores/ai-analysis-store.ts
@src/lib/api/analysis.ts
@src/lib/i18n.tsx
@src/lib/utils.ts
@src/components/TagSuggestionCard.tsx
@src/components/ColorPaletteStrip.tsx
@src/components/AnalysisHistoryList.tsx
@src/components/DetailPanel.tsx
@src/components/ui/skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AiAnalysisSection Container Component</name>
  <files>src/components/AiAnalysisSection.tsx</files>
  <action>
Create `src/components/AiAnalysisSection.tsx` — the main container that orchestrates all AI analysis sub-sections. Named export, consumes `useAiAnalysisStore` for state and `useTranslation` for i18n.

**Props interface:**
```typescript
interface AiAnalysisSectionProps {
  imageId: string
}
```

**Store reads (use selectors for performance):**
```typescript
const result = useAiAnalysisStore(s => s.getResult(imageId))
const state = useAiAnalysisStore(s => s.getState(imageId))
const progress = useAiAnalysisStore(s => s.getProgress(imageId))
const error = useAiAnalysisStore(s => s.getError(imageId))
const triggerAnalysis = useAiAnalysisStore(s => s.triggerAnalysis)
```

**Component has a private `SectionLabel` helper** (matches existing DetailPanel.tsx pattern — repeated as private helper, not exported):
```typescript
function SectionLabel({ children }: { children: React.ReactNode }) {
  return (
    <h3 className="text-[9px] uppercase tracking-[0.1em] text-text-faint font-sans">
      {children}
    </h3>
  )
}
```
Note: AiAnalysisSection should NOT reuse the `SectionLabel` from DetailPanel.tsx — per established pattern (EmbeddingDetailCard.tsx), SectionLabel is replicated as a private helper in each component file.

**Overall section container:**
- Wrapper div: `space-y-3` (12px gap between sub-sections per spec line 46: sm spacing for internal padding)
- Consistent with DetailPanel's existing section spacing

**Render logic based on `state`:**

### State: 'idle' (no analysis ever performed — per spec lines 273-276 section AI-01 states)
1. SectionLabel: `{t("aiAnalysis.sectionLabel")}`
2. Empty state body (per spec lines 576-578 and 600):
   - Text: `p` element with `text-[14px] text-text-muted font-sans leading-relaxed`
   - Content: `{t("aiAnalysis.empty.body")}`
3. CTA Button (per spec lines 450-462, AI-04 idle state):
   - `button` with `h-8 px-5` (32px height, auto width min 120px)
   - Background: `bg-accent` (per spec: accent reserved for CTA button)
   - Text: `text-surface text-[12px] font-sans` (surface = light text on dark accent per spec line 147)
   - Border radius: `rounded-[4px]` (方章 pattern per spec line 196)
   - Hover: `hover:bg-accent-hover`
   - Focus visible: `focus-visible:ring-1 focus-visible:ring-accent/20`
   - Transition: `transition-all duration-200 ease-out`
   - Content: `{t("aiAnalysis.cta.analyze")}`
   - `onClick`: call `triggerAnalysis(imageId)`
   - Margin: `mt-2` (8px below section label, 12px above empty body text — per spec line 471)
   - `disabled` when state is 'analyzing' (prevent double-trigger per spec line 496)

### State: 'analyzing' (per spec lines 459 and 473-485)
1. SectionLabel: `{t("aiAnalysis.sectionLabel")}`
2. Progress bar (per spec lines 473-485):
   - Container: `w-full` (288px within section)
   - Progress bar track: `w-full h-1 rounded-[2px] bg-accent/20` (track: accent at 20% opacity per spec line 482)
   - Progress bar fill: div with dynamic width `style={{ width: `${Math.min(progress, 100)}%` }}`, `h-1 rounded-[2px] bg-accent transition-all duration-200 ease-out`
3. Progress text: `text-[12px] font-sans text-text-muted text-right mt-1` — `{t("aiAnalysis.cta.analyzing")}`
4. No Cancel button — analysis is non-interruptible per spec line 485.

### State: 'complete' (per spec lines 276-277 for description, 361-370 for tags, 384-429 for content, 501-552 for history)
Render all sub-sections with data from `result`:

**Sub-section 0: Section label** — `{t("aiAnalysis.sectionLabel")}`

**Sub-section 1: Description (AI-01 per spec lines 268-284):**
- If `result.description` exists:
  - Heading: `{t("aiAnalysis.description.heading")}` — Label styling (10px uppercase tracking-0.1em text-faint)
  - Description text: `p` with `text-[14px] font-sans text-text-secondary leading-[1.6]` (matches spec line 75: Body 14px, text-secondary, line-height 1.6)
  - Use `result.description` for EN locale, `result.descriptionZh` for ZH locale. Determine locale from `useTranslation()`.
  - Read-only, non-interactive. No hover, no click.

**Sub-section 2: Tag Suggestions (AI-02 per spec lines 289-375):**
- If `result.tags.length > 0`:
  - Heading: `{t("aiAnalysis.tags.heading")}` — Label styling (10px uppercase tracking-0.1em text-faint)
  - Container: `flex flex-col gap-1` (4px gap between cards per spec line 317)
  - Map over `result.tags` — for each tag, render `<TagSuggestionCard key={tag.label} imageId={imageId} tag={tag} />`
  - Filter: Do NOT render cards for tags that are in `rejectedTags` Set (already rejected — card handles its own collapse, but parent should also skip)
  - After all cards: If all tags are either accepted or rejected, show: `p` with `text-[12px] font-sans text-text-faint` — `{t("aiAnalysis.tags.allReviewed")}` (per spec lines 368-370)
- If `result.tags.length === 0`:
  - Text: `{t("aiAnalysis.tags.none")}` — `text-[12px] text-text-faint font-sans` (per spec line 606)

**Sub-section 3: Content Analysis (AI-03 per spec lines 379-445):**
If `result.objects.length > 0 || result.palette.length > 0 || result.composition`:
- Heading: `{t("aiAnalysis.composition.heading")}` — no, wait. Per 006-UI-SPEC, the AI-03 section has its own heading "CONTENT ANALYSIS" — but the i18n keys don't have a "contentAnalysis" heading. Let me check: Actually per spec lines 386-403, the sub-sections within Content Analysis have their own headings. The overall section heading would be implicit. Actually, looking at the spec more carefully, lines 386-387 show:
  ```
  [Sub-section heading: "Content Analysis"]  -- 10px Label, uppercase, text-faint
  ```
  But the i18n contract (lines 638-670) has:
  - `objects.heading`: "OBJECTS DETECTED"
  - `palette.heading`: "COLOR PALETTE"  
  - `composition.heading`: "COMPOSITION"
  
  There's no "Content Analysis" overall heading. The three sub-headings ARE the content analysis. So render them directly without an overarching heading.

**Object labels (AI-03 objects per spec lines 406-413):**
- If `result.objects.length > 0`:
  - Sub-heading: `{t("aiAnalysis.objects.heading")}` — Label styling
  - Container: `flex flex-wrap gap-1` (4px gap per spec line 410)
  - Max 5 objects visible. For each object (first 5): render a `span` chip:
    ```
    <span className="text-[12px] font-sans text-text-secondary bg-bg rounded-[4px] px-2 py-0.5">
      {object}
    </span>
    ```
  - Use `result.objects` for EN, `result.objectsZh` for ZH locale.
  - If more than 5 objects: show `+{overflow} more` in `text-[12px] text-text-muted font-sans` (per spec line 411). Use `t("aiAnalysis.objects.overflow").replace("{n}", String(overflow))`.

**Color palette (AI-03 palette per spec lines 415-423):**
- If `result.palette.length > 0`:
  - Sub-heading: `{t("aiAnalysis.palette.heading")}` — Label styling
  - Render `<ColorPaletteStrip palette={result.palette} />`

**Composition notes (AI-03 composition per spec lines 425-428):**
- If `result.composition`:
  - Sub-heading: `{t("aiAnalysis.composition.heading")}` — Label styling
  - Text: `p` with `text-[14px] font-sans text-text-secondary leading-[1.6]` — same as description text
  - Use `result.composition` for EN, `result.compositionZh` for ZH.

**Sub-section 4: Analysis History (AI-05 per spec lines 501-552):**
- Render `<AnalysisHistoryList imageId={imageId} />` — the component itself handles the null return when empty.

**Sub-section 5: Re-analyze link (AI-04 post-analysis state per spec lines 487-492):**
- After all sub-sections, render a small text link: `{t("aiAnalysis.cta.reanalyze")}`
- Styling: `text-[12px] font-sans text-text-muted hover:text-accent hover:underline transition-colors duration-200 ease-out`
- Right-aligned: `self-end` or `text-right`
- `onClick`: call `triggerAnalysis(imageId)` (re-triggers analysis)
- This replaces the full CTA button after first analysis

### State: 'error' (per spec lines 616-617)
1. SectionLabel: `{t("aiAnalysis.sectionLabel")}`
2. Error text: `p` with `text-[14px] font-sans text-danger text-center` — `{t("aiAnalysis.error.unavailable")}` (per spec line 616)
3. Below error text, two elements:
   - "Retry" text link: `{t("aiAnalysis.cta.retry")}` — `text-[12px] font-sans text-accent hover:underline transition-colors duration-200 ease-out`, `onClick` calls `triggerAnalysis(imageId)` (per spec line 617)
   - CTA button resets to idle state (full button): `{t("aiAnalysis.cta.analyze")}` — same styling as idle CTA, `onClick` calls `triggerAnalysis(imageId)`.

### Fade-in animation for results:
When state transitions from 'analyzing' to 'complete', apply a fade-in effect. Use a local `useState(false)` called `showResults` that becomes true via `useEffect` when state becomes 'complete'. The results container gets: `transition-opacity duration-200 ease-out` + `opacity-0` (when `!showResults`) / `opacity-100` (when `showResults`).

**Export:** `export function AiAnalysisSection({ imageId }: AiAnalysisSectionProps)`

**Imports:**
```typescript
import { useAiAnalysisStore } from "@/stores/ai-analysis-store"
import { useTranslation } from "@/lib/i18n"
import { cn } from "@/lib/utils"
import { TagSuggestionCard } from "@/components/TagSuggestionCard"
import { ColorPaletteStrip } from "@/components/ColorPaletteStrip"
import { AnalysisHistoryList } from "@/components/AnalysisHistoryList"
```

No icons. No lucide-react. All labels are text per project rules.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>AiAnalysisSection renders all AI analysis sub-sections based on store state: empty state + CTA (idle), progress bar (analyzing), description + tag cards + content analysis + history + re-analyze link (complete), error text + retry (error). Project type-checks with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate AiAnalysisSection into DetailPanel</name>
  <files>src/components/DetailPanel.tsx</files>
  <action>
Modify `src/components/DetailPanel.tsx` to insert `<AiAnalysisSection>` between the Tags section and the existing Analysis section, following the section order from 006-UI-SPEC.md lines 226-243.

**Current section order (from reading DetailPanel.tsx lines 97-176):**
1. Tags section (lines 99-113)
2. Analysis section (lines 115-145, conditional on `image.analysis`)
3. EmbeddingDetailCard (line 148)
4. Score section (lines 150-163, conditional on `image.score`)
5. Actions section (lines 166-175)

**New section order (per 006-UI-SPEC.md lines 226-243):**
1. Tags section
2. **[NEW] AI Analysis section** — between Tags and existing Analysis
3. Analysis section
4. EmbeddingDetailCard
5. Score section
6. Actions section

**Changes to make:**

1. **Add import** at top:
   ```typescript
   import { AiAnalysisSection } from "@/components/AiAnalysisSection"
   ```

2. **After the Tags section's closing `</div>`** (after line 113) and **before the `{image.analysis && (` block** (line 116), insert:
   ```tsx
   <div className="h-px bg-border-subtle mx-4" />
   <AiAnalysisSection imageId={image.id} />
   ```

3. **Remove** the existing `<div className="h-px bg-border-subtle mx-4" />` that was between Tags and Analysis (line 115, the one right before `{image.analysis && ...`) — this separator is now moved to before AiAnalysisSection.

Actually, re-examine the current code. The existing structure:
```
Line 97: {/* Tags */}
Line 99-113: Tags div
Line 115: {image.analysis && (   → separator is INSIDE this conditional block
Line 116: <div className="h-px... → actually wait
```

Let me re-read lines 115-146 more carefully. Actually from what I read:
```
Line 115: {/* Analysis */}
Line 116: {image.analysis && (
Line 117:   <>
Line 118:     <div className="h-px bg-border-subtle mx-4" />
Line 119:     <div className="p-4 space-y-2.5">
...
Line 145:   </>
Line 146: )}
```

So the separator before Analysis section (line 118) is INSIDE the conditional block. After Tags section ends at line 113, there's no unconditional separator before the Analysis conditional.

**The modification should be:**

After the Tags `</div>` (after line 113) and before the `{image.analysis && ...}` analysis block (line 116), insert:

```tsx
<div className="h-px bg-border-subtle mx-4" />
<AiAnalysisSection imageId={image.id} />
```

This adds:
- A separator above the AI Analysis section (standard section separator pattern used throughout DetailPanel)
- The AiAnalysisSection itself (which renders section label + content, wrapping in its own `space-y-3`)

The AiAnalysisSection does NOT add its own bottom divider — the next section (Analysis) has its own conditional separator above it. This keeps sections visually separated.

**No other changes.** Do not modify:
- Tags section
- Analysis section (generation metadata)
- EmbeddingDetailCard
- Score section
- Actions section
- Header, Image preview, Rating & Favorite, File Info sections

**The `image.id` value** is already available — DetailPanel destructures `detailImage` as `image` on line 14, and `image.id` is the `img-N` string format.

Export remains unchanged: `export function DetailPanel()` — no props change, no interface change.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>DetailPanel renders AiAnalysisSection between Tags and existing Analysis section. AiAnalysisSection receives `imageId={image.id}`. Section order matches 006-UI-SPEC.md specification. Project type-checks with zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User button click → triggerAnalysis | CTA click initiates mock analysis flow — pure client-side state changes, no network boundary crossed. |
| Store state → DetailPanel render | Analysis results rendered as React children — JSX auto-escapes all text content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-006-05 | Tampering | AiAnalysisSection/CTA onClick | accept | onClick only calls triggerAnalysis(imageId) — id is a read-only prop from DetailPanel, not user input. |
| T-006-06 | Denial of Service | triggerAnalysis/progress interval | accept | setInterval at 200ms is lightweight. Store's progressIntervals Map is keyed per imageId — only one interval per image at a time. No memory leak: interval is cleared on completion, error, or component unmount (not needed — store handles lifecycle). |
| T-006-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced in this plan. Existing dependencies only. |
</threat_model>

<verification>
- [ ] `src/components/AiAnalysisSection.tsx` exists — renders all states (idle, analyzing, complete, error) with correct sub-sections
- [ ] `src/components/DetailPanel.tsx` contains `<AiAnalysisSection imageId={image.id} />` between Tags and Analysis sections
- [ ] Section order matches 006-UI-SPEC.md: Tags -> AI Analysis -> Analysis -> Embedding -> Score -> Actions
- [ ] Full project type-checks: `npx tsc --noEmit` passes with zero errors
- [ ] Visual verification: `npm run dev`, open DetailPanel on any image, confirm AI Analysis section appears between Tags and existing Analysis section
</verification>

<success_criteria>
1. AiAnalysisSection renders complete state machine: idle (empty + CTA), analyzing (progress bar), complete (description + tag cards + content + history + re-analyze link), error (error text + retry + CTA reset)
2. DetailPanel section order: Header -> Image -> Rating/Favorite -> File Info -> Tags -> **AI Analysis** -> Analysis -> Embedding -> Score -> Actions
3. "Analyze with AI" CTA button (accent bg, 4px radius, 12px text) triggers analysis flow with progress bar feedback
4. Tag accept/reject interaction works: Accept adds tag to acceptedTags Set, card transitions to success state then collapses. Reject collapses immediately.
5. Full project type-checks with zero errors
</success_criteria>

<output>
Create `.planning/phases/006-ai-analysis-panel/006-03-SUMMARY.md` when done
</output>
